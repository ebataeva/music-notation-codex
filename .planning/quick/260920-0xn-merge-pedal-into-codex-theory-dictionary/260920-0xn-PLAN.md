---
phase: quick-260920-0xn
plan: 01
type: execute
wave: 1
depends_on: [260831-s8m, 260901-p4d, 260916-bta]
files_modified:
  - core/theory/explainer.py
  - app/pages/loop_coach.py
  - app/services/generation.py
  - core/models.py
  - core/engine/loop_engine.py
  - tests/test_theory_musical_correctness.py
  - tests/test_models.py
  - .planning/STATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Branch merge/pedal-into-theory contains both pedal (ee30c7f) and codex/theory-dictionary (bbe0a8c) with no conflict markers"
    - "tests/ is green without deleting or skipping tests; the golden baseline is not recaptured"
    - "tests-ui/ is green: pedal's guides, real notation width and three cards; Codex's dictionary links and authored duet"
    - "The three cards read differently: each short section is 1-2 sentences and the first sentence describes this take's line"
    - "how_to_end names the note the repeat lands on (loop seam), in the long text and on the card"
    - "KEY-01 has a single path: the preset carries the player's key; engine, score and explanation read it there"
    - "READ-01 (<=4 notes per bar) and Codex's 'explanation supported by actual notes' coexist: the inventory says what was supplied and what each bar sounds"
  artifacts:
    - path: "core/theory/explainer.py"
      provides: "Codex base + _absolute_semitone, _melodic_shape_clause, _loop_seam_clause; inventory with played notes"
      contains: "_loop_seam_clause"
    - path: "core/models.py"
      provides: "GenerationTrace.played_pitches"
      contains: "played_pitches"
    - path: "app/pages/loop_coach.py"
      provides: "Codex base + pedal panels and card order; _render_theory_section with dictionary links"
      contains: "_render_theory_section"
  key_links:
    - from: "core/engine/loop_engine.py::generate_variant_from_progression"
      to: "core/theory/explainer.py::_chord_inventory"
      via: "GenerationTrace.played_pitches"
    - from: "app/services/generation.py::generate_loop_variants"
      to: "core/engine/loop_engine.py::generate_variants"
      via: "preset replaced with the player's key (normalize_context), no key kwargs"
---

# Quick Task 260920-0xn: merge pedal into codex/theory-dictionary

Source of the decisions: `handoffs/context-handoff-20260920-002257.md` (audit of both branches, 2026-09-20).

## Tasks

1. **Merge and resolve the four conflict files.** `git merge pedal` on a new branch from `codex/theory-dictionary`; expected conflicts: `.planning/STATE.md`, `app/pages/loop_coach.py`, `app/services/generation.py`, `core/theory/explainer.py`. Codex is the base for `explainer.py` and `loop_coach.py`; pedal's additions come back on top. `generation.py` keeps both import blocks and one KEY-01 path.
2. **Reconcile READ-01 with the chord inventory.** Add `played_pitches` to the trace, make the inventory say what was supplied and what each bar sounds, rewrite Codex's 12 score/explanation agreement tests to "played notes are a subset of chord tones".
3. **Short cards.** The per-take shape sentence leads `why_it_works` and the seam sentence ends `how_to_end`, in both the long text and the short card sections; every card section stays at 1-2 sentences.

## Verification

- `.venv/bin/python -m pytest tests/ -q` green, golden regression green without recapture.
- `.venv/bin/python -m pytest tests-ui/ -q` green.
- App in the browser: three cards with notation and audio, different first sentences, seam named in "How to end", progression suggestions panel, pedalboard and Sheeran guides, dictionary links.

## Out of scope

- Duet presets ignoring the entered progression (open question for the user).
- The shared spelling bug (A7 in D minor printed as Db instead of C#) present in both branches.
- Push, PR, merge to main.
