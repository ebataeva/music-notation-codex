---
phase: quick-260715-3fo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/loop_coach.py
  - tests/test_loop_coach_audio.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "OSMD <script> tag is present in the page HTML immediately after create_loop_coach_page() builds the page, not only after clicking Generate"
    - "Clicking Generate no longer blocks the NiceGUI event loop for the duration of FluidSynth rendering"
    - "Clicking a MusicXML/MIDI/WAV export button triggers ui.download with the file bytes as the first positional argument, not a content= keyword"
    - "Storage-restore path (page reload after a prior generation) still renders variant cards without errors"
    - "Duet rehearsal-loop audio players (violin/cello/full tracks) still render unchanged"
  artifacts:
    - path: "app/pages/loop_coach.py"
      provides: "OSMD script loaded once at page build; async do_generate via run.io_bound; ui.download called positionally"
      contains: "_load_osmd_script"
    - path: "tests/test_loop_coach_audio.py"
      provides: "Regression tests for OSMD script timing and ui.download positional-arg contract"
      contains: "test_export_buttons_call_ui_download_with_positional_bytes"
  key_links:
    - from: "app/pages/loop_coach.py::create_loop_coach_page"
      to: "app/pages/loop_coach.py::_load_osmd_script"
      via: "direct call during page build (before Generate can be clicked)"
      pattern: "_load_osmd_script\\(\\)"
    - from: "app/pages/loop_coach.py::do_generate"
      to: "nicegui.run.io_bound"
      via: "await run.io_bound(generate_loop_variants, ...)"
      pattern: "run\\.io_bound\\(generate_loop_variants"
    - from: "app/pages/loop_coach.py::_render_variant_export"
      to: "nicegui.ui.download"
      via: "positional bytes as first argument"
      pattern: "ui\\.download\\(\\s*c"
---

<objective>
Fix three confirmed production bugs in the Loop Coach page that make it effectively unusable: notation never renders (OSMD script loaded too late), the UI freezes for 1-3 minutes during generation (sync FluidSynth call on the event loop), and export buttons crash (`ui.download` called with a nonexistent `content=` kwarg on the installed NiceGUI 3.14.0 API).

Purpose: Restore the three core interactive flows (see notation, generate without freezing, download files) on branch `hotfix/loop-coach-notation-audio` without touching any other app surface (duet audio rendering, storage-restore rendering, theory text) — all verified working today and must stay working.

Output: `app/pages/loop_coach.py` fixed; `tests/test_loop_coach_audio.py` extended with regression tests that lock in the OSMD script-loading timing and the `ui.download` positional-argument contract; full `tests/` suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/pages/loop_coach.py
@tests/test_loop_coach_audio.py
@app/services/generation.py

Confirmed facts (do not re-derive):
- Installed NiceGUI `ui.download` signature: `ui.download(src: str | Path | bytes, filename: str | None = None, media_type: str = '') -> None` — no `content` kwarg exists in this version.
- `nicegui.run.io_bound` exists and is the correct choice here (FluidSynth renders WAV via a subprocess, not CPU-bound Python).
- `generate_loop_variants(chord_progression: str, preset_name: str, seed: int | None = None, include_audio: bool = False, count: int = 3) -> list[dict]` — current call site in `do_generate` already passes matching kwargs; only the sync-vs-async wrapping changes.
- `ui.add_body_html` only takes effect during initial page build; calling it from a click handler after the page has been delivered (current bug, inside `_render_variant_cards`) is a silent no-op — verified in a live headless-browser repro: `window.opensheetmusicdisplay` stays `false` forever after Generate.
- `_render_variant_cards` is called from two places: the Generate click handler (`do_generate`) and the storage-restore path at the bottom of `create_loop_coach_page`. Both must keep working after moving the OSMD script tags out of it.
- `tests/test_loop_coach_audio.py` uses a hand-rolled `_FakeUi`/`_FakeElement` pair, monkeypatched onto `loop_coach.ui`, to unit-test render helpers without a real NiceGUI/browser context. Extend this pattern; do not introduce Playwright/browser tests here (those live in `tests-ui/`, out of scope for this hotfix).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix OSMD load timing, async generation, and ui.download calls</name>
  <files>app/pages/loop_coach.py</files>
  <action>
    Apply three fixes to `app/pages/loop_coach.py`, all scoped to this one file:

    1. OSMD script timing. Extract a new module-level function `_load_osmd_script() -> None` that contains exactly the two `ui.add_body_html(...)` calls currently at the top of `_render_variant_cards` (the OSMD CDN `<script src="{OSMD_JS_URL}">` tag and the `<script>{OSMD_INIT_SCRIPT}</script>` tag). Remove those two lines from `_render_variant_cards` entirely (keep `container.clear()` and the rest of that function unchanged). Call `_load_osmd_script()` exactly once, near the top of `create_loop_coach_page()` (e.g. right after the page header labels, before the input row), so the `<script>` tags land in the initial page HTML delivered on page build — not after a later click handler runs.

    2. Async generation. Change `from nicegui import app, ui` to `from nicegui import app, run, ui`. Change `def do_generate():` to `async def do_generate():`. Replace the direct synchronous call `results = generate_loop_variants(chord_progression=chord_input.value, preset_name=mood_select.value, include_audio=True, count=3)` with `results = await run.io_bound(generate_loop_variants, chord_progression=chord_input.value, preset_name=mood_select.value, include_audio=True, count=3)`. Leave everything else in `do_generate` (the in-flight guard, disable/enable calls, spinner, storage writes, status text) unchanged — NiceGUI supports async event handlers registered via `.on("click", do_generate)` unchanged, no wrapper needed.

    3. `ui.download` positional argument. In `_render_variant_export`, for all three download buttons (MusicXML, MIDI, WAV), change `ui.download(content=c, filename=..., media_type=...)` to `ui.download(c, filename=..., media_type=...)` — i.e. drop the `content=` keyword so the bytes are passed as the first positional argument (`src`). Do not change the `filename=` or `media_type=` keyword arguments, and do not touch the SVG button (it does not use `ui.download`, it builds a client-side Blob URL — leave it exactly as is).

    Do not modify `_render_variant_audio`, `_render_variant_notation`'s JS rendering logic, `_theory_sections`, `_render_single_variant`, `do_example`, or the storage-restore block at the bottom of `create_loop_coach_page` beyond what's needed for fix #1.
  </action>
  <verify>
    <automated>.venv/bin/python -m py_compile app/pages/loop_coach.py && .venv/bin/pytest tests/test_loop_coach_audio.py -q</automated>
  </verify>
  <done>File compiles cleanly; existing tests in tests/test_loop_coach_audio.py (duet/solo/missing audio rendering) still pass unchanged; `_load_osmd_script` exists as a standalone function; `do_generate` is `async def` and awaits `run.io_bound(generate_loop_variants, ...)`; all three `ui.download` calls in `_render_variant_export` pass bytes positionally with no `content=` kwarg.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for OSMD timing and ui.download contract, confirm full suite green</name>
  <files>tests/test_loop_coach_audio.py</files>
  <action>
    Extend `tests/test_loop_coach_audio.py` using the existing `_FakeUi`/`_FakeElement` monkeypatch pattern (`monkeypatch.setattr(loop_coach, "ui", fake_ui)`).

    Add `import base64` to the top of the file (needed for constructing fake MIDI/WAV b64 payloads in the new test).

    Extend `_FakeUi` with:
    - `self.add_body_html_calls: list[str] = []` and a method `add_body_html(self, content: str)` that appends to it.
    - `self.buttons: list` and a method `button(self, text, color=None)` returning a new `_FakeButton(text)` instance, appended to `self.buttons`.
    - `self.downloads: list[tuple]` and a method `download(self, *args, **kwargs)` that appends `(args, kwargs)`.
    - `row(self, *args, **kwargs)` returning a `_FakeRow()` instance (supports use as `with ui.row().classes(...):`).
    - `run_javascript(self, _script)` that is a no-op (returns None) — needed because `_render_variant_export`'s SVG button lambda references `ui.run_javascript` at button-creation time is fine, but the click callback itself calls it only when invoked; having the no-op present avoids AttributeError if a test happens to invoke it.

    Add two small helper classes near `_FakeElement`:
    - `_FakeButton`: `__init__(self, label)` stores `self.label = label` and `self.callbacks = {}`; `on(self, event, callback)` stores `self.callbacks[event] = callback` and returns `self`; `props(self, _value)` returns `self`.
    - `_FakeRow`: `classes(self, _value)` returns `self`; `__enter__(self)` returns `self`; `__exit__(self, *args)` returns `False`.
    - `_FakeContainer`: `clear(self)` is a no-op; `__enter__(self)` returns `self`; `__exit__(self, *args)` returns `False`.

    Add test `test_load_osmd_script_adds_body_html_exactly_once(monkeypatch)`: monkeypatch `loop_coach.ui` with a fresh `_FakeUi`, call `loop_coach._load_osmd_script()`, assert `len(fake_ui.add_body_html_calls) == 2`, assert `loop_coach.OSMD_JS_URL in fake_ui.add_body_html_calls[0]`, assert `"loadOsmd" in fake_ui.add_body_html_calls[1]`.

    Add test `test_render_variant_cards_does_not_call_add_body_html(monkeypatch)`: monkeypatch `loop_coach.ui` with a fresh `_FakeUi`, call `loop_coach._render_variant_cards([], _FakeContainer())`, assert `fake_ui.add_body_html_calls == []` — locks in that the OSMD script tags are no longer emitted from this function (fix #1 regression guard).

    Add test `test_export_buttons_call_ui_download_with_positional_bytes(monkeypatch)`: monkeypatch `loop_coach.ui` with a fresh `_FakeUi`, call `loop_coach._render_variant_export(0, {"musicxml_string": "<score/>", "midi_bytes_b64": base64.b64encode(b"midi-bytes").decode("ascii"), "wav_bytes_b64": base64.b64encode(b"wav-bytes").decode("ascii")})`. Assert `len(fake_ui.buttons) == 4` (MusicXML, MIDI, WAV, SVG in that order). Invoke the captured `"click"` callback on each of the first 3 buttons (`fake_ui.buttons[0].callbacks["click"]()`, etc — do not invoke the SVG button's callback, it calls `ui.run_javascript` with different semantics and is out of scope). Assert `len(fake_ui.downloads) == 3`. For each `(args, kwargs)` in `fake_ui.downloads`: assert `len(args) == 1` and `"content" not in kwargs` (proves the bytes are positional, not keyword). Assert `fake_ui.downloads[0][0][0] == "<score/>".encode("utf-8")` and `fake_ui.downloads[0][1]["filename"] == "cello_loop_variant_1.musicxml"`. Assert `fake_ui.downloads[1][0][0] == b"midi-bytes"`. Assert `fake_ui.downloads[2][0][0] == b"wav-bytes"`.

    After writing the tests, run the full suite to confirm zero regressions across the whole project (not just this file).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_loop_coach_audio.py -q && .venv/bin/pytest tests/ -q</automated>
  </verify>
  <done>tests/test_loop_coach_audio.py has 3 new passing tests covering OSMD script-load timing (once, at page build, not from _render_variant_cards) and the ui.download positional-bytes contract; full `tests/` suite passes with zero failures (baseline 184 passed, expect 187 passed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser <-> NiceGUI server | User-typed chord progression and mood/key selections cross into `generate_loop_variants`; already validated upstream (parse_progression), unchanged by this hotfix |
| Server -> Browser (export) | Base64-encoded MusicXML/MIDI/WAV bytes flow from server-generated content into `ui.download`; content is server-generated (music21/FluidSynth output), not user-supplied, so no new injection surface from this fix |
| Server -> Browser (OSMD) | MusicXML string is base64-encoded before embedding in `ui.run_javascript` (unchanged by this hotfix) to avoid HTML/JS injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-quick260715-3fo-01 | Denial of Service (UI freeze) | app/pages/loop_coach.py::do_generate | mitigate | Move FluidSynth-backed generation off the event loop via `await run.io_bound(...)`, restoring UI responsiveness during the 1-3 minute render; no behavior change to what is generated |
| T-quick260715-3fo-02 | Denial of Service (silent feature failure) | app/pages/loop_coach.py::_render_variant_cards | mitigate | OSMD script now loads once during page build via `_load_osmd_script()`, closing the "script tag added after page delivery is a no-op" failure mode; regression test locks this in |
| T-quick260715-3fo-03 | Tampering (malformed API call crashing export) | app/pages/loop_coach.py::_render_variant_export | mitigate | `ui.download` now called with the correct positional signature matching the installed NiceGUI 3.14.0 API, eliminating the TypeError observed in server logs |

No new external inputs, no new dependencies, no package installs — package legitimacy gate not applicable to this plan.
</threat_model>

<verification>
1. `.venv/bin/python -m py_compile app/pages/loop_coach.py` — file is syntactically valid.
2. `.venv/bin/pytest tests/test_loop_coach_audio.py -q` — existing + 3 new tests pass.
3. `.venv/bin/pytest tests/ -q` — full suite green, 0 failures (baseline 184 passed -> expect 187 passed).
4. `grep -n "ui.add_body_html" app/pages/loop_coach.py` — the two calls appear only inside `_load_osmd_script`, not inside `_render_variant_cards`.
5. `grep -n "async def do_generate" app/pages/loop_coach.py` — confirms the handler is async.
6. `grep -n "content=" app/pages/loop_coach.py` — returns no matches (the `content=` kwarg is fully removed from all `ui.download` calls).
</verification>

<success_criteria>
- OSMD `<script>` tag is present in the page HTML immediately when `create_loop_coach_page()` builds the page (not only after Generate is clicked).
- `do_generate` is `async def` and calls generation via `await run.io_bound(generate_loop_variants, ...)`, so the event loop is not blocked during FluidSynth rendering.
- All three export buttons (MusicXML, MIDI, WAV) call `ui.download` with file bytes as the first positional argument, no `content=` kwarg.
- Storage-restore path and duet rehearsal-loop audio rendering are unchanged and still pass their existing tests.
- `.venv/bin/pytest tests/ -q` reports 0 failures.
</success_criteria>

<output>
Create `.planning/quick/260715-3fo-hotfix-loop-coach-ui-osmd-script-never-l/260715-3fo-SUMMARY.md` when done
</output>
