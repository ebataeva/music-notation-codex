---
phase: quick
plan: 260706-21y
type: execute
wave: 1
depends_on: []
files_modified:
  - core/engine/loop_engine.py
  - core/models.py
  - core/presets/mood_presets.py
  - core/presets/registry.py
  - tests/test_loop_engine.py
  - tests/test_models.py
  - tests/test_presets_registry.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SAFE-01, PLAT-02]

must_haves:
  truths:
    - "generate_variant, generate_variant_from_progression, and build_duet_score all raise ValueError when the produced note count exceeds MAX_NOTES (512), matching SAFE-02's raise-based convention"
    - "No MOOD_PRESETS[*].feel value contains Cyrillic text; LoopVariant.label built from feel is always English"
    - "get_preset() returns a MoodPreset whose mutable-looking fields cannot corrupt the shared MOOD_PRESETS registry when mutated by a caller"
    - "REQUIREMENTS.md traceability maps SAFE-01 to Phase 2/LoopEngine as satisfied, not orphaned to Phase 1"
    - "Full test suite (88 pre-existing + new guard/regression tests) stays green"
  artifacts:
    - path: "core/engine/loop_engine.py"
      provides: "MAX_NOTES constant and a note-count guard applied at all 4 generation entry points (build_score/generate_variant, build_duet_score, build_progression_score/generate_variant_from_progression)"
      contains: "MAX_NOTES = 512"
    - path: "core/presets/mood_presets.py"
      provides: "English-only feel strings for all 7 presets"
    - path: "core/models.py"
      provides: "MoodPreset with tuple-based collection fields (deep-immutable) instead of list/dict"
    - path: "core/presets/registry.py"
      provides: "get_preset() that cannot leak a mutable alias to shared MOOD_PRESETS data"
  key_links:
    - from: "core/engine/loop_engine.py"
      to: "SAFE-02 MAX_BARS guard pattern"
      via: "same raise-based ValueError convention, same 4 entry points"
      pattern: "MAX_NOTES"
    - from: ".planning/REQUIREMENTS.md"
      to: "core/engine/loop_engine.py"
      via: "traceability row SAFE-01 -> Phase 2/LoopEngine"
      pattern: "SAFE-01.*Phase 2"
---

<objective>
Close three tech-debt tails from `.planning/v1-MILESTONE-AUDIT.md`:

1. **SAFE-01 (orphaned)**: implement a max-notes-per-variant guard (512 notes) in
   `core/engine/loop_engine.py`, alongside the existing SAFE-02 `MAX_BARS` guard,
   covering all 4 generation entry points (solo, duet, progression-build,
   progression-variant). Remap SAFE-01 in `.planning/REQUIREMENTS.md` traceability
   from Phase 1 to Phase 2/LoopEngine and mark it satisfied.
2. **PLAT-02 risk**: translate the Russian `MOOD_PRESETS[*].feel` strings
   (`core/presets/mood_presets.py`) to English, since `feel` is spliced verbatim
   into `LoopVariant.label` at `loop_engine.py:139,509,513` and Phase 4 will render
   that label as UI copy.
3. **WR-05 (01-REVIEW.md)**: `frozen=True` on `MoodPreset` doesn't protect its
   `list`/`dict` collection fields from mutation; `get_preset()` hands out the
   shared instance. Convert collection fields to tuples (dicts to
   `dict[str, tuple[...]]`) so the dataclass is genuinely immutable, and add a
   regression test proving a caller cannot corrupt `MOOD_PRESETS` through a
   returned preset.

Purpose: Close out interim v1 audit gaps before Phase 3 planning continues, so
the milestone's requirement traceability and safety-guard coverage are accurate
before more phases build on top of `core/`.

Output: Updated `core/engine/loop_engine.py` (MAX_NOTES guard), `core/models.py`
(tuple-based `MoodPreset`), `core/presets/mood_presets.py` (English `feel`
strings), `core/presets/registry.py` (unaffected — still simple lookup, now
safe by construction), updated tests, updated `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@core/engine/loop_engine.py
@core/models.py
@core/presets/mood_presets.py
@core/presets/registry.py
@tests/test_loop_engine.py
@tests/test_models.py
@tests/test_presets_registry.py

Existing SAFE-02 guard pattern to mirror (do not restructure, extend
alongside it): `MAX_BARS = 64` module constant in `core/engine/loop_engine.py`,
checked with a `raise ValueError(...)` at the top of `build_score`,
`generate_variant`, `build_duet_score`, `build_progression_score`, and
`generate_variant_from_progression` — before any `stream.Score`/measure
construction happens, per SAFE-10 (guards must be code, not comments).

`feel` is read-only in production code: only referenced in
`scripts/generate_cello_dark_ostinato.py` (`print(f"...{preset.feel}")`,
stdout only, not baked into MusicXML/MIDI) and spliced into
`LoopVariant.label` at `core/engine/loop_engine.py:139` (`generate_variant`),
`:509/513` (`generate_variant_from_progression`). No golden-regression hash
depends on `feel` text (`tests/test_golden_regression.py` hashes MIDI bytes
and normalized MusicXML, neither of which embeds `feel`).

`tests/test_presets_registry.py:29` currently asserts the Russian string
verbatim: `MOOD_PRESETS["dark_trip_hop"].feel == "темный, сексуальный,
петлевой trip-hop groove"` — this assertion must be updated to the new
English string, not deleted (it is the one live regression check on `feel`
content).

`MoodPreset` fields to convert list->tuple: `rhythm`, `bars` (`list[list[str]]`
-> `tuple[tuple[str, ...], ...]`), `progressions`, `modulations`, `mood_tips`.
Dict fields `duet_rhythm: dict[str, list[float]] | None` and
`duet_bars: dict[str, list[list[str]]] | None` keep their outer `dict` (keyed
`"cello"`/`"violin"`) but their values become tuples:
`dict[str, tuple[float, ...]] | None` and
`dict[str, tuple[tuple[str, ...], ...]] | None`. All production call sites
(`core/engine/loop_engine.py`, `core/presets/registry.py`, the 4 CLI scripts)
only read via `len()`, `zip()`, indexing, or iteration — no `.append`/mutation
exists anywhere in `core/` or `scripts/`, confirmed by grep, so this is a
drop-in-safe conversion requiring only the literal `[...]` -> `(...)` edits in
`core/presets/mood_presets.py` and the type annotations in `core/models.py`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MAX_NOTES guard (SAFE-01) to all 4 LoopEngine entry points</name>
  <files>core/engine/loop_engine.py, tests/test_loop_engine.py</files>
  <behavior>
    - Test 1: `generate_variant` raises `ValueError` when a synthetic preset's
      `bars` x `rhythm` combination produces more than 512 notes (mirror the
      existing `test_generate_variant_raises_for_more_than_64_bars_before_score_built`
      pattern: build a synthetic `MoodPreset` whose total note count exceeds
      512 while staying under the 64-bar cap, e.g. 64 bars x 9 notes/bar = 576
      notes, rhythm summing to 4.0 with 9 equal fractional durations).
    - Test 2: `generate_variant` does NOT raise for a preset at or under 512
      notes (e.g. reuse `get_preset("dark_trip_hop")`, which is 8 bars x 8
      notes = 64 notes, well under the cap) — proves the guard doesn't
      false-positive on real presets.
    - Test 3: `generate_variant_from_progression` raises `ValueError` when
      `len(chords) * notes_per_bar` exceeds 512 (construct via
      `parse_progression` with enough repeated chords and a preset with a
      long rhythm, or directly assert via a preset with `rhythm` long enough
      that even a moderate chord count crosses 512).
    - Test 4: `build_duet_score` raises `ValueError` when the combined cello +
      violin note count exceeds 512 (synthetic preset with oversized
      `duet_bars`/`duet_rhythm` for one part).
    - Test 5: `build_duet_score` does NOT raise for the existing `sexy_duet`/
      `simple_sexy_duet`/`dorian_sexy_duet` presets (regression guard — reuse
      existing passing tests as the "no false positive" proof; no new test
      needed if Tests 1-8 in the existing suite still pass, but add one
      explicit assertion that combined note count for `sexy_duet` is well
      under 512).
  </behavior>
  <action>
    Add `MAX_NOTES = 512` as a module constant in `core/engine/loop_engine.py`,
    directly below the existing `MAX_BARS = 64` constant, with a comment
    referencing SAFE-01 (mirror the SAFE-02 comment style: "Maximum notes a
    single generated variant may contain (SAFE-01: denial-of-service guard
    against unbounded per-variant note density, independent of the bar-count
    guard above)."). Add a small module-level helper
    `_total_note_count(bars: list[list[str]] | tuple[tuple[str, ...], ...]) -> int`
    that sums `len(bar)` across all bars — used by all 4 entry points instead
    of duplicating the sum expression.

    In `build_score`: after the existing `MAX_BARS` check, add a
    `MAX_NOTES` check using `_total_note_count(preset.bars)` before
    `validate_bar_duration` runs; raise
    `ValueError(f"Requested variant has {count} notes, exceeding the maximum of {MAX_NOTES}.")`
    on violation.

    In `generate_variant`: add the same `MAX_NOTES` check immediately after
    its existing `MAX_BARS` check (before `resolved_seed, _rng =
    _resolve_seed(seed)`), using `_total_note_count(preset.bars)` — this
    mirrors the existing pattern of checking bars in both `build_score` and
    `generate_variant` (defense in depth per the SAFE-02 comment at line
    113-114).

    In `build_duet_score`: after the existing per-part `MAX_BARS` loop (lines
    191-195), add a combined check:
    `total_notes = _total_note_count(cello_bars) + _total_note_count(violin_bars)`,
    raise `ValueError` if `total_notes > MAX_NOTES`, using a message naming
    both parts' contribution (e.g. "Duet variant has {total_notes} notes
    (cello + violin combined), exceeding the maximum of {MAX_NOTES}.").
    Place this check before `validate_bar_duration` calls.

    In `build_progression_score`: after the existing `MAX_BARS` check (line
    422-423) and the `not chords` check, but before `_resolve_seed`, compute
    `total_notes = len(chords) * len(preset.rhythm)` (one bar per chord,
    `notes_per_bar` = `len(preset.rhythm)`, matching the function's own later
    `notes_per_bar = len(preset.rhythm)` line) and raise `ValueError` if it
    exceeds `MAX_NOTES`.

    In `generate_variant_from_progression`: add the identical
    `len(chords) * len(preset.rhythm)` check immediately after its own
    existing `MAX_BARS` check (before `resolved_seed, _rng =
    _resolve_seed(seed)`) — same defense-in-depth pairing as
    `build_score`/`generate_variant`. Guard against `preset.rhythm` being
    empty (duet-only preset) by placing this check AFTER the existing
    `not chords` check but note that the duet-only-preset `not preset.rhythm`
    check happens later inside `build_progression_score` — since
    `generate_variant_from_progression` calls `build_progression_score`
    internally, do not duplicate the empty-rhythm check here; only add the
    `MAX_NOTES` check guarded by `if preset.rhythm and len(chords) *
    len(preset.rhythm) > MAX_NOTES`, so a duet-only preset with empty rhythm
    still reaches `build_progression_score`'s own actionable "no solo rhythm"
    error instead of a spurious `MAX_NOTES` message.

    All raises are plain `ValueError` (SAFE-10 convention, matching every
    existing guard in this file) — no new exception types.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_loop_engine.py -v</automated>
  </verify>
  <done>
    MAX_NOTES = 512 constant exists in core/engine/loop_engine.py; all 4
    generation entry points (build_score/generate_variant,
    build_duet_score, build_progression_score/generate_variant_from_progression)
    raise ValueError when total note count exceeds 512; existing presets
    (all 7 in MOOD_PRESETS) still generate without raising; new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Translate MOOD_PRESETS[*].feel to English; make MoodPreset deep-immutable (WR-05)</name>
  <files>core/models.py, core/presets/mood_presets.py, tests/test_models.py, tests/test_presets_registry.py</files>
  <action>
    In `core/models.py`: change `MoodPreset`'s field type annotations from
    `list`/`dict[str, list[...]]` to tuples: `rhythm: tuple[float, ...]`,
    `bars: tuple[tuple[str, ...], ...]`, `progressions: tuple[str, ...]`,
    `modulations: tuple[str, ...]`, `mood_tips: tuple[str, ...]`,
    `duet_rhythm: dict[str, tuple[float, ...]] | None = None`,
    `duet_bars: dict[str, tuple[tuple[str, ...], ...]] | None = None`. Keep
    `frozen=True`. Add a one-line comment above the dataclass referencing
    WR-05: "Collection fields are tuples, not lists (WR-05: frozen=True alone
    does not protect mutable list/dict fields; get_preset() must not be able
    to leak a mutation path into the shared MOOD_PRESETS registry)."

    In `core/presets/mood_presets.py`: convert every `MoodPreset(...)`
    construction's list-literal arguments (`rhythm=[...]`, `bars=[...]`,
    `progressions=[...]`, `modulations=[...]`, `mood_tips=[...]`) from `[...]`
    to `(...)` tuple-literal syntax, and every `duet_rhythm={...}`/
    `duet_bars={...}` dict's inner list values from `[...]` to `(...)` — for
    all 7 presets. Translate every `feel="..."` value from Russian to
    English, preserving the descriptive meaning:
    - `dark_trip_hop`: "dark, sexy, loopy trip-hop groove"
    - `ritual_tribal`: "ritual pulse with accents, more body and movement"
    - `noir_slow_burn`: "slow noir, unspoken tension, held pause"
    - `driving_cinematic`: "fast cinematic motor, drive and build"
    - `sexy_duet`, `simple_sexy_duet`: leave `feel=""` (already empty, no
      Russian text to translate)
    - `dorian_sexy_duet`: already English ("D Dorian vamp: Dm9 -> G9. The B
      natural keeps it warm instead of funeral-dark.") — leave unchanged.

    Do NOT translate `progressions`/`modulations`/`mood_tips` Russian theory
    text — those are out of this task's scope (not spliced into
    `LoopVariant.label`, not yet consumer-facing; Phase 3 TheoryExplainer
    will address them directly when it's built, per its own requirements
    THEORY-01/02).

    In `tests/test_presets_registry.py`: update the assertion at line 29 from
    the Russian string to
    `MOOD_PRESETS["dark_trip_hop"].feel == "dark, sexy, loopy trip-hop groove"`.
    Add a new test `test_no_preset_feel_contains_cyrillic` that iterates all
    `MOOD_PRESETS.values()` and asserts no `preset.feel` character falls in
    the Cyrillic Unicode range (`0x0400`-`0x04FF`) — a durable regression
    guard so future presets can't reintroduce non-English `feel` text.

    In `tests/test_models.py`: update `test_mood_preset_defaults_and_frozen`
    to construct its synthetic `MoodPreset` with tuple literals instead of
    list literals (`rhythm=(0.5,) * 8`, `bars=(("C2",) * 8,)`,
    `progressions=("i - VI - v - i",)`, etc.) so it exercises the new field
    types. Add a new test `test_get_preset_mutation_does_not_corrupt_registry`
    in `tests/test_presets_registry.py` that calls `get_preset("dark_trip_hop")`
    twice, attempts `preset.bars[0]` indexing (read-only proof) and asserts
    `isinstance(preset.bars, tuple)` and `isinstance(preset.rhythm, tuple)` —
    proving the returned object's collections are immutable tuples, not
    mutable lists a caller could corrupt in place (tuples have no
    `.append`/`__setitem__`, so this is a structural guarantee, not a
    behavioral one requiring `pytest.raises(TypeError)` on mutation attempts
    — but also add `with pytest.raises(TypeError): preset.bars[0] = ("X9",)`
    to make the immutability explicit and executable).
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_models.py tests/test_presets_registry.py -v</automated>
  </verify>
  <done>
    All 7 MOOD_PRESETS entries use tuple literals for rhythm/bars/
    progressions/modulations/mood_tips and tuple values in duet_rhythm/
    duet_bars; all 4 solo presets' feel strings are English; MoodPreset's
    dataclass field types are tuples; get_preset()'s returned object's
    collection fields are provably immutable (TypeError on item assignment);
    no MOOD_PRESETS[*].feel contains Cyrillic characters.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full suite, update REQUIREMENTS.md traceability for SAFE-01, verify golden regression</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    In `.planning/REQUIREMENTS.md`: change the SAFE-01 checkbox in the
    "Specification Gaming Guards (SAFE)" section from `- [ ]` to `- [x]`.
    Update the Traceability table row for SAFE-01 from
    `| SAFE-01 | Phase 1 | Gap — Phase 1 closed without it; no max-notes guard
    in code (see v1-MILESTONE-AUDIT.md) |` to
    `| SAFE-01 | Phase 2 | Complete — max-notes guard (MAX_NOTES=512) added to
    LoopEngine alongside SAFE-02's MAX_BARS guard, covers all 4 generation
    entry points (2026-07-06 quick task) |`. Update the trailing
    "Last updated" footer line to append a note about this remap (append,
    don't replace the existing 2026-07-06 audit note): add a new line "SAFE-01
    remapped to Phase 2/LoopEngine and marked Complete (max-notes guard
    implemented + tested, quick task 260706-21y)."

    Then run the full test suite once to confirm the combined effect of
    Tasks 1 and 2 (MAX_NOTES guard + tuple-based MoodPreset + English feel
    strings) keeps every pre-existing test green, including the golden
    regression suite (which subprocess-invokes the 5 CLI scripts — those
    scripts read `preset.feel`/`preset.bars`/`preset.rhythm` via attribute
    access only, so the tuple conversion and feel translation must not
    change MIDI/MusicXML bytes, only stdout text).
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest -q</automated>
  </verify>
  <done>
    .planning/REQUIREMENTS.md SAFE-01 checkbox is [x], traceability row shows
    Phase 2/Complete; full test suite (88 pre-existing + new tests from Tasks
    1-2) passes with zero failures, including
    tests/test_golden_regression.py's byte-identity checks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> LoopEngine generation functions | Internal trust boundary: any code path (future UI, future MCP-driven regeneration) that calls generate_variant/build_duet_score/generate_variant_from_progression with attacker-influenced bar/rhythm data must be stopped by the MAX_BARS/MAX_NOTES guards before Score construction, matching the SAFE-01/SAFE-02 threat model of the 111-agent incident referenced in REQUIREMENTS.md |
| Caller -> get_preset() return value | Internal trust boundary: any caller receiving a MoodPreset from the registry must not be able to mutate the shared MOOD_PRESETS global through the returned reference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Denial of Service | core/engine/loop_engine.py generation entry points | mitigate | MAX_NOTES=512 raise-based ValueError guard at all 4 entry points, mirroring the existing SAFE-02 MAX_BARS pattern (Task 1) |
| T-quick-02 | Tampering | core/models.py MoodPreset / core/presets/registry.py get_preset() | mitigate | Convert list/dict collection fields to tuples so frozen=True is structurally honest; a caller cannot mutate a returned preset's bars/rhythm/etc. in place (Task 2, WR-05) |
| T-quick-03 | Information Disclosure | core/presets/mood_presets.py feel text rendered as UI copy in a future phase | mitigate | Translate feel to English now, before Phase 4 wires LoopVariant.label into any rendered UI (PLAT-02) |
</threat_model>

<verification>
Run the full suite after each task:
`.venv/bin/python3 -m pytest -q`

Task-specific checks:
- `.venv/bin/python3 -m pytest tests/test_loop_engine.py -v` — MAX_NOTES guard tests pass, all 4 entry points covered.
- `.venv/bin/python3 -m pytest tests/test_models.py tests/test_presets_registry.py -v` — tuple immutability and English-feel tests pass.
- `.venv/bin/python3 -m pytest tests/test_golden_regression.py -v` — MIDI/MusicXML byte-identity unaffected by the tuple conversion or feel translation.
- `grep -c "Cyrillic\|темный\|ритуальный\|нуар\|кинематографичн" core/presets/mood_presets.py` after Task 2 should show 0 occurrences of the old feel strings (progressions/modulations/mood_tips Russian text is expected to remain — out of scope).
</verification>

<success_criteria>
- MAX_NOTES = 512 guard exists in core/engine/loop_engine.py and is enforced (ValueError) at all 4 generation entry points (build_score/generate_variant, build_duet_score, build_progression_score/generate_variant_from_progression).
- No MOOD_PRESETS[*].feel value contains Cyrillic text.
- MoodPreset's collection fields (rhythm, bars, progressions, modulations, mood_tips, duet_rhythm values, duet_bars values) are tuples, not lists; get_preset() cannot leak a mutable-in-place reference to shared data.
- .planning/REQUIREMENTS.md SAFE-01 is checked and its traceability row points to Phase 2/LoopEngine as Complete.
- Full pytest suite (88 pre-existing tests + new tests added by this plan) passes with zero failures.
</success_criteria>

<output>
Create `.planning/quick/260706-21y-fix-v1-audit-tech-debt-tails-safe-01-max/260706-21y-SUMMARY.md` when done.
</output>
