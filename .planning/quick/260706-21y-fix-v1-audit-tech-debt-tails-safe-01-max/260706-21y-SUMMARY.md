---
phase: quick
plan: 260706-21y
subsystem: core-engine
tags: [python, dataclasses, music21, safety-guards, i18n]

requires:
  - phase: "02"
    provides: "SAFE-02 MAX_BARS guard pattern in core/engine/loop_engine.py to mirror"
provides:
  - "SAFE-01 MAX_NOTES=512 denial-of-service guard at all 4 LoopEngine generation entry points"
  - "English-only MOOD_PRESETS prose: feel AND progressions/modulations/mood_tips (no Cyrillic anywhere in core/, scripts/, tests/, apps/)"
  - "Deep-immutable MoodPreset (tuple-based collection fields, get_preset() cannot leak a mutable alias)"
  - "English-only CLI output in scripts/harmony_advisor.py"
affects: [phase-3-theoryexplainer, phase-4-ui]

tech-stack:
  added: []
  patterns: ["module-level _total_note_count() helper shared across 4 guard call sites", "tuple-of-tuples for deep-immutable dataclass collection fields"]

key-files:
  created: []
  modified:
    - core/engine/loop_engine.py
    - core/models.py
    - core/presets/mood_presets.py
    - tests/test_loop_engine.py
    - tests/test_models.py
    - tests/test_presets_registry.py
    - .planning/REQUIREMENTS.md

key-decisions:
  - "MAX_NOTES check placed after each existing MAX_BARS check, mirroring the SAFE-02 defense-in-depth pairing exactly (Task 1)"
  - "progressions/modulations/mood_tips theory text initially deferred (Task 2), then translated in the coordinator-directed follow-up (Task 4) after a Cyrillic sweep widened the PLAT-02 scope"
  - "generate_variant_from_progression's MAX_NOTES check is guarded by `if preset.rhythm` so duet-only presets still reach build_progression_score's own actionable error instead of a spurious MAX_NOTES message"

requirements-completed: [SAFE-01, PLAT-02]

duration: 25min
completed: 2026-07-06
---

# Quick Task 260706-21y: Close v1 Audit Tech-Debt Tails Summary

**Added SAFE-01's MAX_NOTES=512 denial-of-service guard to all 4 LoopEngine generation entry points, translated all MOOD_PRESETS feel strings to English, and converted MoodPreset's collection fields to tuples for genuine immutability (WR-05).**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-07-05T23:35:00Z (approx, first commit 01:35 local)
- **Completed:** 2026-07-05T23:42:36Z (last commit 01:42 local)
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments

- `MAX_NOTES = 512` guard now enforced at `build_score`/`generate_variant`, `build_duet_score`, and `build_progression_score`/`generate_variant_from_progression` — mirrors the existing SAFE-02 `MAX_BARS` pattern exactly, using a shared `_total_note_count()` helper.
- All 4 solo `MOOD_PRESETS[*].feel` strings translated from Russian to English (`dark_trip_hop`, `ritual_tribal`, `noir_slow_burn`, `driving_cinematic`); duet presets' `feel` was already empty or English and untouched.
- `MoodPreset`'s collection fields (`rhythm`, `bars`, `progressions`, `modulations`, `mood_tips`, and the values inside `duet_rhythm`/`duet_bars`) converted from `list`/`dict[str, list]` to `tuple`/`dict[str, tuple]`, making `frozen=True` structurally honest — `get_preset()` can no longer hand out a reference a caller could mutate in place.
- `.planning/REQUIREMENTS.md` SAFE-01 checkbox checked and traceability remapped from "Phase 1 / Gap" to "Phase 2/LoopEngine / Complete".
- Full pytest suite grew from 88 to 95 tests, all green, including the golden-regression byte-identity suite (`tests/test_golden_regression.py`).
- **Follow-up (coordinator-directed):** all remaining Russian prose translated to English — 24 `progressions`/`modulations`/`mood_tips` strings across the 4 solo presets, `harmony_advisor.py` CLI output literals, and the identical Russian path-setup comment in the 4 generator scripts. Cyrillic sweep of `core/`, `scripts/`, `tests/`, `apps/` is now fully clean.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add MAX_NOTES guard (SAFE-01) to all 4 LoopEngine entry points** - `75480d2` (feat)
2. **Task 2: Translate MOOD_PRESETS[*].feel to English; make MoodPreset deep-immutable (WR-05)** - `7deb00a` (feat)
3. **Task 3: Run full suite, update REQUIREMENTS.md traceability for SAFE-01** - `c72eb4b` (docs)
4. **Task 4 (follow-up): Translate remaining preset theory prose and CLI output to English (PLAT-02)** - `77594d2` (feat)

**Plan metadata:** handled by orchestrator (SUMMARY.md/STATE.md commit not included above per executor constraints).

## Files Created/Modified

- `core/engine/loop_engine.py` - `MAX_NOTES = 512` constant, `_total_note_count()` helper, guard checks added to all 4 generation entry points
- `core/models.py` - `MoodPreset` field types changed from list/dict-of-list to tuple/dict-of-tuple
- `core/presets/mood_presets.py` - all 7 preset constructions converted to tuple literals; 4 solo presets' `feel` translated to English; all 24 `progressions`/`modulations`/`mood_tips` strings translated to English (follow-up); docstring updated (no longer claims verbatim Russian migration)
- `scripts/harmony_advisor.py` - CLI output literals translated to English: `--list-genres` header, intro line, 3 `print_section` headers (follow-up)
- `scripts/generate_cello_dark_ostinato.py`, `scripts/generate_sexy_duet_loop.py`, `scripts/generate_simple_sexy_duet_loop.py`, `scripts/generate_dorian_sexy_duet_loop.py` - Russian path-setup comment translated to English per CLAUDE.md locked decision (follow-up)
- `tests/test_loop_engine.py` - 5 new tests (MAX_NOTES raise/no-raise for solo, progression, and duet paths); 1 existing test assertion fixed for tuple-vs-list comparison
- `tests/test_models.py` - `test_mood_preset_defaults_and_frozen` updated to construct with tuple literals
- `tests/test_presets_registry.py` - Russian `feel` assertion updated to English; 2 new tests (`test_no_preset_feel_contains_cyrillic`, `test_get_preset_mutation_does_not_corrupt_registry`); duet rhythm/bars assertions updated to tuple literals
- `.planning/REQUIREMENTS.md` - SAFE-01 checkbox checked, traceability row and footer updated

## Decisions Made

- Mirrored the SAFE-02 `MAX_BARS` guard placement and message style exactly for `MAX_NOTES` (same raise-based `ValueError` convention, same 4 entry points, same defense-in-depth double-check in `generate_variant`/`generate_variant_from_progression`) — no new exception types, no restructuring of existing guard flow.
- `progressions`/`modulations`/`mood_tips` theory text was initially left untranslated per the plan's scope; a coordinator-directed follow-up (read-only Cyrillic sweep widened the PLAT-02 tail) then translated all of it, plus `harmony_advisor.py` CLI output, before merge — Phase 3 TheoryExplainer now receives English-only prose.
- In the follow-up, also translated the identical Russian code comment in the 4 generator scripts (CLAUDE.md locked decision: code comments in English) — small CLAUDE.md-driven scope extension that leaves the code-directory Cyrillic sweep fully clean.
- `generate_variant_from_progression`'s new MAX_NOTES check only fires `if preset.rhythm` (non-empty), so a duet-only preset still reaches `build_progression_score`'s own actionable "no solo rhythm" error rather than a confusing MAX_NOTES message for an empty-rhythm preset.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `test_generate_variant_chord_tones_used_matches_bars` broke after tuple conversion**
- **Found during:** Task 2 (post-implementation full-suite verification)
- **Issue:** `generate_variant`'s trace builds `chord_tones_used` via `list(pitches)` per bar (matching `GenerationTrace.chord_tones_used: list[list[str]]`'s intentional list-of-list contract), but the pre-existing test compared each `bar_tones` list directly against `preset.bars`'s now-tuple bar, and `["a"] != ("a",)` in Python even with identical contents.
- **Fix:** Updated the test assertion to `assert bar_tones == list(expected_bar)`, comparing contents rather than container type. `GenerationTrace`'s list-of-list type contract is intentionally unchanged (only `MoodPreset`'s fields were in scope for the tuple conversion).
- **Files modified:** `tests/test_loop_engine.py`
- **Verification:** Full suite re-run, 95/95 passed.
- **Committed in:** `7deb00a` (part of Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug fix, direct consequence of Task 2's in-scope tuple conversion)
**Impact on plan:** No scope creep — the fix was strictly required to keep the pre-existing test suite green after the plan's own Task 2 change, as required by the plan's own success criteria ("Full pytest suite ... passes with zero failures").

## Issues Encountered

None beyond the auto-fixed test assertion above.

**Out-of-scope pre-existing working-tree state:** Several `scores/musicxml/*.musicxml` files were already modified in the working tree before this task began (non-deterministic `<encoding-date>`/`<score-part id>` metadata from a prior script run, unrelated to any file this plan touches). Left uncommitted and logged in `deferred-items.md` in this directory, per the executor's scope-boundary rule.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- SAFE-01 tech-debt tail is closed: guard implemented, tested, and traced in REQUIREMENTS.md. No blockers for Phase 3 (TheoryExplainer) planning.
- `MoodPreset` is now genuinely immutable end-to-end; any future phase (Phase 3 TheoryExplainer, Phase 4 UI) consuming `get_preset()` output can rely on it being safe to hold without defensive copying.
- English `feel` strings are ready for Phase 4's UI rendering of `LoopVariant.label` — no further translation work needed for that field.

## Self-Check: PASSED

- FOUND: core/engine/loop_engine.py
- FOUND: core/models.py
- FOUND: core/presets/mood_presets.py
- FOUND: tests/test_loop_engine.py
- FOUND: tests/test_models.py
- FOUND: tests/test_presets_registry.py
- FOUND: .planning/REQUIREMENTS.md
- FOUND commit 75480d2
- FOUND commit 7deb00a
- FOUND commit c72eb4b

---
*Phase: quick*
*Completed: 2026-07-06*
