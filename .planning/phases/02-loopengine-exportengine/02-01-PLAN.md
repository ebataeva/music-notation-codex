---
phase: 02-loopengine-exportengine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/engine/validators.py
  - tests/test_validators.py
autonomous: true
requirements: [LOOP-01]

must_haves:
  truths:
    - "validate_pitch rejects octave-less pitch names with ValueError instead of silently defaulting to octave 4"
    - "validate_pitch never lets a raw music21 PitchException escape; malformed names raise ValueError"
    - "validate_bar_duration rejects any individual non-positive duration, not just a bad sum"
    - "The full existing pytest suite (including golden regression) still passes after the change"
  artifacts:
    - path: "core/engine/validators.py"
      provides: "Hardened validate_pitch / validate_bar_duration (WR-02, WR-03 fixes)"
      contains: "raise ValueError"
    - path: "tests/test_validators.py"
      provides: "New test cases proving WR-02/WR-03 fixes"
  key_links:
    - from: "tests/test_validators.py"
      to: "core/engine/validators.py"
      via: "import validate_pitch, validate_bar_duration"
      pattern: "from core\\.engine\\.validators import"
---

<objective>
Harden `core/engine/validators.py` per Phase 1 code review findings WR-02 and WR-03 (01-REVIEW.md), so that when Phase 2's LoopEngine calls these validators during score assembly (D-06), they reject malformed pitch/rhythm data with the documented `ValueError` contract instead of silently accepting bad data or leaking a raw music21 exception.

Purpose: This is a prerequisite for D-06 ("Phase 1 validators are called INSIDE the engine during score assembly") — the engine must be able to trust `ValueError` as the only failure mode from these functions before wiring them in (Plan 02-02).
Output: Hardened `core/engine/validators.py`, extended `tests/test_validators.py` proving both fixes, full existing test suite still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-loopengine-exportengine/02-CONTEXT.md
@.planning/phases/02-loopengine-exportengine/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix WR-02 (octave-less names + PitchException leak) and WR-03 (non-positive rhythm values)</name>
  <files>core/engine/validators.py, tests/test_validators.py</files>
  <read_first>
    - core/engine/validators.py (current implementation, lines 1-29 — exact current state to modify in place)
    - tests/test_validators.py (current 17 test functions — flat function style, no test classes, `pytest.raises(ValueError)` — extend this file, do not restructure it)
    - .planning/phases/01-core-library-skeleton-validators/01-REVIEW.md (WR-02 section: `Pitch("C").midi == 60` with `octave=None` silently passes; `Pitch("H2")` raises `music21.pitch.PitchException`, not `ValueError`. WR-03 section: `validate_bar_duration([5.0, -1.0], "4/4")` and `[4.0, 0.0, 0.0]` both pass the sum check today)
    - .planning/phases/02-loopengine-exportengine/02-PATTERNS.md (lines 236-274: exact reference fix shapes for both WR-02 and WR-03, including the caveat that "the exact octave-detection check is planner/implementer's call" and the WR-03 empty-list message improvement)
  </read_first>
  <behavior>
    - Test: `validate_pitch("C")` (no octave digit) raises `ValueError` (currently silently passes at octave 4 — this is the WR-02 regression case)
    - Test: `validate_pitch("Bb")` (no octave digit, has accidental letter but no digit) raises `ValueError`
    - Test: `validate_pitch("H2")` (invalid pitch letter) raises `ValueError`, NOT `music21.pitch.PitchException` — assert via `pytest.raises(ValueError)` with no other exception type escaping
    - Test: `validate_pitch("C2")`, `validate_pitch("C#3")`, `validate_pitch("Bb2")` (existing valid cases) still pass with no raise — regression guard for the fix not over-rejecting valid octave-bearing names
    - Test: `validate_bar_duration([5.0, -1.0], "4/4")` raises `ValueError` (sum is 4.0, currently passes silently — WR-03 regression case)
    - Test: `validate_bar_duration([4.0, 0.0, 0.0], "4/4")` raises `ValueError` (sum is 4.0, contains zero durations)
    - Test: `validate_bar_duration([], "4/4")` raises `ValueError` with a message naming the empty-rhythm problem (not just "0 != 4.0")
    - Test: existing bar-duration passing/failing cases (4/4 match, 3/4 mismatch, 3/4 match, sexy_duet 7-element rhythm) still behave identically — regression guard
  </behavior>
  <action>
    In `validate_pitch`: before constructing `pitch.Pitch(pitch_name)`, reject any `pitch_name` with no digit character (octave-less names like `"C"`, `"Bb"`) by raising `ValueError` naming the input and stating an octave is required (e.g. reference form: "Pitch {pitch_name!r} must include an octave (e.g. 'C3')"). Wrap the `pitch.Pitch(pitch_name)` construction in `try/except` catching `music21.pitch.PitchException` (import `from music21.pitch import PitchException` or catch broad `Exception` per 01-REVIEW.md's WR-02 fix recipe) and re-raise as `ValueError` with the original exception chained via `from exc`. Keep the existing MIDI-range check and its message format unchanged. Do not introduce a new exception class — `ValueError` only, matching the codebase-wide convention (01-PATTERNS.md: "all raised errors are ValueError... no custom exception classes exist anywhere in the codebase").
    In `validate_bar_duration`: before the existing sum-comparison logic, add a guard that raises `ValueError` when `rhythm` is empty (message must name the meter signature and state the rhythm is empty, not just report "0 != expected"), and a guard that raises `ValueError` when any element of `rhythm` is `<= 0` (message should include the offending list so the failure is debuggable). Keep the existing sum/tolerance comparison logic and its message format unchanged for the case where all individual elements are positive but the sum is wrong.
    Do not change either function's signature (`validate_pitch(pitch_name: str, extended: bool = False) -> None`, `validate_bar_duration(rhythm: list[float], meter_signature: str, tolerance: float = 1e-9) -> None`) — Plan 02-02 will call these exact signatures from `loop_engine.py`.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_validators.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_validators.py -v` passes with zero failures, including the new WR-02/WR-03 test cases listed in `<behavior>`
    - `core/engine/validators.py` contains no bare `except Exception:` that swallows without re-raising as `ValueError`
    - `grep -c "raise ValueError" core/engine/validators.py` returns at least 4 (octave-less guard, PitchException wrap, empty-rhythm guard, non-positive guard) plus the pre-existing range/sum-mismatch raises
    - No new custom exception class defined anywhere in `core/engine/validators.py`
  </acceptance_criteria>
  <done>validate_pitch rejects octave-less names and never leaks PitchException; validate_bar_duration rejects empty/non-positive rhythm entries; all existing and new tests in tests/test_validators.py pass.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm full suite (including golden regression) stays green after the validator hardening</name>
  <files>tests/test_golden_regression.py (read-only verification, no modification expected)</files>
  <read_first>
    - tests/test_golden_regression.py (byte-identity guard — no preset data in core/presets/mood_presets.py should trip either new WR-02/WR-03 check per D-14's "verified not to affect golden output files")
    - core/presets/mood_presets.py (confirm no pitch name in any `bars`/`duet_bars` entry is octave-less or malformed, and every `rhythm`/`duet_rhythm` entry has strictly positive values — spot-check the one known exception, `simple_sexy_duet` duet_bars["cello"][1] == ["A1", "E2", "G2", "C#3"], which is a range violation not a malformed-name violation and is NOT validated at generation time yet in this plan since validators are not wired into any script or engine until Plan 02-02)
  </read_first>
  <action>
    Run the full existing pytest suite to confirm the Task 1 changes do not break any currently-passing test, especially `tests/test_golden_regression.py` (byte-identity of MIDI/MusicXML output from the unmodified scripts) and `tests/test_presets_registry.py` (asserts on `MOOD_PRESETS` data, including the known `"A1"` legacy note). No production code changes in this task — this is a verification-only task. If any regression is found, fix it in `core/engine/validators.py` (Task 1's file) before proceeding, since the golden suite is a hard constraint per the phase quality gate.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/` reports zero failures across the entire suite
    - `tests/test_golden_regression.py::test_midi_outputs_match_baseline` passes
    - `tests/test_golden_regression.py::test_musicxml_outputs_match_baseline_normalized` passes
    - `tests/test_presets_registry.py` passes unchanged (no preset data required modification)
  </acceptance_criteria>
  <done>Entire pytest suite (17+ existing tests plus Task 1's new tests) passes with zero failures; golden regression confirms the validator hardening has no effect on script output since validators are not yet called from any generation path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preset data -> validators | `core/presets/mood_presets.py` pitch/rhythm literals flow into `validate_pitch`/`validate_bar_duration` once wired (Plan 02-02); this plan only hardens the validators themselves, no caller wiring yet |
| Test input -> validators | Test-authored strings (including deliberately malformed ones) exercise the validators directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `validate_pitch` input | mitigate | Reject octave-less/malformed pitch names before music21 construction; never let PitchException escape as an undocumented failure mode |
| T-02-02 | Information Disclosure | `validate_bar_duration` error messages | accept | Messages include the offending rhythm list; local single-user CLI/library, no untrusted multi-user exposure, low-value target |
| T-02-03 | Denial of Service | Malformed pitch/rhythm causing uncaught exception crash | mitigate | All failure paths funnel to `ValueError` with a clear message rather than an uncaught music21 internal exception |
| T-02-SC | Tampering | pip/requirements installs | accept | No new package installs in this plan (music21 already pinned in requirements.txt from Phase 1) |
</threat_model>

<verification>
Run `.venv/bin/python3 -m pytest tests/ -v` — expect zero failures across the full suite, including the golden regression byte-identity guard.
</verification>

<success_criteria>
- `core/engine/validators.py` raises `ValueError` (never a raw music21 exception) for octave-less names, malformed pitch strings, empty rhythm lists, and non-positive rhythm durations.
- All pre-existing valid-input and invalid-input test cases in `tests/test_validators.py` still pass with unchanged behavior.
- Full test suite including `tests/test_golden_regression.py` passes with zero failures.
</success_criteria>

<output>
Create `.planning/phases/02-loopengine-exportengine/02-01-SUMMARY.md` when done
</output>
