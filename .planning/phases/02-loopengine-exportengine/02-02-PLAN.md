---
phase: 02-loopengine-exportengine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - core/engine/loop_engine.py
  - core/export/exporter.py
  - core/export/__init__.py
  - core/presets/registry.py
  - scripts/generate_cello_dark_ostinato.py
  - tests/test_loop_engine.py
  - tests/test_exporter.py
  - tests/test_presets_registry.py
autonomous: true
requirements: [LOOP-01, SAFE-02, SAFE-07]

must_haves:
  truths:
    - "Calling build_score(preset) with a solo MoodPreset returns a music21 Score object with no errors (ROADMAP success criterion 1)"
    - "Calling ExportEngine.export_to_musicxml() and export_to_midi() writes valid files to scores/musicxml/ and scores/midi/ (ROADMAP success criterion 2)"
    - "scripts/generate_cello_dark_ostinato.py runs via its thin wrapper and produces identical output to before the refactor (ROADMAP success criterion 3)"
    - "Generation accepts an explicit seed; the same seed reproduces an identical variant; when no seed given, one is generated and always recorded in the trace (ROADMAP success criterion 4, D-01/D-02)"
    - "Every generated variant carries a populated GenerationTrace: pattern_strategy, register_choices, chord_tones_used are non-None and non-empty for a solo preset (ROADMAP success criterion 5, D-04)"
    - "generate_cello_dark_ostinato.py --genre sexy_duet is no longer offered as a choice (WR-01 fix verified)"
    - "A request for more than 64 bars raises an error before any Score is built (SAFE-02)"
  artifacts:
    - path: "core/engine/loop_engine.py"
      provides: "build_score(preset, seed=None) -> music21 Score, plus generate_variant(preset, seed=None) -> LoopVariant with populated trace"
      contains: "def build_score"
    - path: "core/export/exporter.py"
      provides: "ExportEngine class with export_to_musicxml/export_to_midi (or combined export) writing to configurable base_dir"
      contains: "class ExportEngine"
    - path: "scripts/generate_cello_dark_ostinato.py"
      provides: "Thin wrapper: parse_args + core calls + printing only, no music21 score-building logic"
      exports: ["main", "parse_args"]
    - path: "tests/test_loop_engine.py"
      provides: "Unit tests for build_score, seed reproducibility, trace population, legacy exception skip, SAFE-02 guard"
    - path: "tests/test_exporter.py"
      provides: "Unit tests for ExportEngine writing to tmp_path base_dir"
  key_links:
    - from: "scripts/generate_cello_dark_ostinato.py"
      to: "core/engine/loop_engine.py"
      via: "import build_score"
      pattern: "from core\\.engine\\.loop_engine import"
    - from: "scripts/generate_cello_dark_ostinato.py"
      to: "core/export/exporter.py"
      via: "import ExportEngine"
      pattern: "from core\\.export\\.exporter import ExportEngine"
    - from: "core/engine/loop_engine.py"
      to: "core/engine/validators.py"
      via: "validate_pitch / validate_bar_duration called during score assembly"
      pattern: "validate_(pitch|bar_duration)\\("
---

<objective>
Extract the solo cello ostinato generation logic from `scripts/generate_cello_dark_ostinato.py` into `core/engine/loop_engine.py` (`build_score`, `generate_variant`) and `core/export/exporter.py` (`ExportEngine`), wire in Phase 1's hardened validators (D-06), add seed plumbing (D-01/D-02/D-03) and full GenerationTrace population (D-04), skip the one known legacy out-of-range note via a per-preset exception mechanism (D-07), fix WR-01 (filter solo-only presets from the CLI), and turn the script into a thin wrapper (D-11/D-12) — while keeping its output byte-identical (golden regression stays green).

Purpose: This is the phase's primary vertical slice — matches ROADMAP success criteria 1-5 end to end for the solo (non-duet) generation path. Phase 2.5 will extend `build_score`'s seed usage; Phase 3's TheoryExplainer will consume the trace this plan populates.
Output: `core/engine/loop_engine.py`, `core/export/exporter.py`, refactored `scripts/generate_cello_dark_ostinato.py`, new unit tests, golden regression still green for the 4 solo-genre invocations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-loopengine-exportengine/02-CONTEXT.md
@.planning/phases/02-loopengine-exportengine/02-PATTERNS.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build core/engine/loop_engine.py (build_score, seed plumbing, trace, legacy exception, SAFE-02/SAFE-07) with unit tests</name>
  <files>core/engine/loop_engine.py, tests/test_loop_engine.py, core/presets/registry.py, tests/test_presets_registry.py</files>
  <read_first>
    - scripts/generate_cello_dark_ostinato.py (current `build_cello_ostinato` lines 18-44 — exact source of the extraction)
    - core/models.py (locked dataclass shapes: `MoodPreset`, `LoopVariant`, `GenerationTrace` — do not modify field names or types)
    - core/engine/validators.py (post-Plan-02-01 hardened `validate_pitch`/`validate_bar_duration` signatures — call these, do not reimplement)
    - core/presets/registry.py (current `get_preset`/`list_presets` — add `list_solo_presets` here, WR-01 fix)
    - core/presets/mood_presets.py (confirm the exact `simple_sexy_duet` A1 legacy note location: `duet_bars["cello"][1] == ["A1", "E2", "G2", "C#3"]` — this is a duet preset, not solo, so the legacy-exception mechanism must key on the correct preset name and note; also confirm none of the 4 solo presets' `bars`/`rhythm` data contain values that would trip validate_pitch/validate_bar_duration once wired in)
    - tests/test_presets_registry.py (current test style — flat functions, `SOLO_MOODS`/`DUET_MOODS` sets already defined at module level; extend for list_solo_presets)
    - .planning/phases/02-loopengine-exportengine/02-PATTERNS.md (lines 27-156: exact extraction pattern for build_score, D-01/D-02 seed resolution shape, D-04 trace accumulation shape, D-07 legacy exception dict shape, D-06 validator wiring point)
  </read_first>
  <behavior>
    - Test: `build_score(get_preset("dark_trip_hop"))` returns a `music21.stream.Score` instance with no exception raised
    - Test: the returned Score has exactly `len(preset.bars)` measures in its cello part (8 measures for dark_trip_hop)
    - Test: `build_score(get_preset("dark_trip_hop"), seed=42)` called twice produces two Scores whose exported MIDI bytes are identical (seed reproducibility — ROADMAP success criterion 4); use `music21.midi.translate.streamToMidiFile` bytes comparison or re-render via ExportEngine to tmp_path and compare file bytes
    - Test: `generate_variant(get_preset("dark_trip_hop"), seed=42).trace.seed == 42`
    - Test: `generate_variant(get_preset("dark_trip_hop"))` (no seed passed) produces a `LoopVariant` whose `trace.seed is not None` (D-02: seed is always recorded, never None)
    - Test: `generate_variant(get_preset("dark_trip_hop")).trace.pattern_strategy` is a non-empty string (e.g. `"preset_verbatim"`)
    - Test: `generate_variant(get_preset("dark_trip_hop")).trace.register_choices` is a non-empty list with one entry per bar (length == `len(preset.bars)`)
    - Test: `generate_variant(get_preset("dark_trip_hop")).trace.chord_tones_used` is a non-empty list of lists, one list of pitch names per bar, matching `preset.bars` content
    - Test: the D-07 legacy exception for `simple_sexy_duet`'s `"A1"` note works — do NOT call `build_score(get_preset("simple_sexy_duet"))` for this (that preset has `rhythm == []`, so the empty-rhythm guard from validate_bar_duration fires first, unrelated to A1); instead call the internal skip-check helper directly (e.g. `_is_legacy_exception("simple_sexy_duet", "A1") is True` and `_is_legacy_exception("dark_trip_hop", "A1") is False`) or construct a minimal synthetic MoodPreset with non-empty rhythm/bars containing `"A1"` under the name `simple_sexy_duet` — proving the exception path exists and is scoped to the documented preset/note only
    - Test: `build_score` on a preset with an out-of-range pitch NOT covered by the legacy exception list raises `ValueError` (proves validate_pitch (D-06) is actually wired in, not bypassed for every case)
    - Test (SAFE-02): calling the engine's variant-generation entry point with a request for more than 64 bars raises an error (e.g. `ValueError`) before any Score object is constructed
    - Test (registry): `list_solo_presets()` returns exactly the 4 solo mood names (`dark_trip_hop`, `ritual_tribal`, `noir_slow_burn`, `driving_cinematic`) and excludes the 3 duet-only presets
  </behavior>
  <action>
    Create `core/engine/loop_engine.py` with `from __future__ import annotations` and imports limited to `music21` submodules actually used (`clef, duration, instrument, key, meter, note, stream, tempo`) plus `random`, plus `core.models.MoodPreset`/`GenerationTrace`/`LoopVariant`, plus `core.engine.validators.validate_bar_duration`/`validate_pitch`. No `argparse`, no `streamlit`, no `sys.path` bootstrap (import-boundary rule, enforced by `tests/test_import_boundary.py`).

    Implement `build_score(preset: MoodPreset, seed: int | None = None) -> stream.Score` by extracting the body of `build_cello_ostinato` from `scripts/generate_cello_dark_ostinato.py` (Score/Part construction, instrument/clef/tempo/key/meter setup, per-bar note construction using `zip(pitches, preset.rhythm, strict=True)` — preserve this exact pairing convention). Before constructing each `note.Note(pitch_name)`, call `validate_pitch(pitch_name)` UNLESS `(preset.name, pitch_name)` matches the legacy exception table (D-07) — implement the legacy exception as a module-level dict `_LEGACY_PITCH_EXCEPTIONS: dict[str, set[str]] = {"simple_sexy_duet": {"A1"}}` keyed by preset name, checked via a small helper (naming is your discretion, e.g. `_is_legacy_exception(preset_name, pitch_name)`); when skipped, emit `warnings.warn(...)` naming the preset and pitch. Call `validate_bar_duration(preset.rhythm, preset.meter_signature)` once per preset (rhythm/meter are preset-level, not per-bar, in current data).

    Add seed resolution per D-01/D-02: implement `_resolve_seed(seed: int | None) -> tuple[int, random.Random]` — if `seed is None`, generate one via `random.Random().getrandbits(32)`; always return `(resolved_seed, random.Random(resolved_seed))`. The current preset-verbatim strategy does not need the `random.Random` instance for actual randomness yet (D-01: "even though the current preset-verbatim strategy is deterministic and won't use it") — thread it through the call anyway so Phase 2.5 can consume it without an interface change.

    Add SAFE-02 guard: before building a Score, if the preset (or an explicit `bars` request parameter, if you choose to add one) implies more than 64 bars, raise `ValueError` naming the limit. Since current solo presets are 8 bars, this guard is a forward-looking assertion — implement it as a check on `len(preset.bars) > 64` inside `build_score` (or `generate_variant`), not merely documented.

    Add SAFE-07 guard: ensure `generate_variant`/`build_score` contains no recursive call to itself or to a "generate more variants" helper — a one-line assertion or simply the absence of such a call satisfies this; add a code comment noting the flat-generation invariant is enforced by the function's structure (no self-recursion), not by documentation alone, per SAFE-07's wording.

    Implement `generate_variant(preset: MoodPreset, seed: int | None = None) -> LoopVariant` (D-05 high-level API): calls `build_score`, resolves the seed via `_resolve_seed`, accumulates trace data DURING the per-bar build loop (D-04) — `register_choices: list[str]` (one label per bar, e.g. `"low register"`/`"mid register"` derived from the bar's note range, or simply the bar index if a register-classification heuristic isn't warranted — Claude's discretion, but must be non-empty and one entry per bar) and `chord_tones_used: list[list[str]]` (append `list(pitches)` per bar verbatim). Set `trace.pattern_strategy = "preset_verbatim"` (or your chosen literal per D-04's "preset_verbatim"-style identifier" guidance — pick one and use it consistently). Set `trace.voice_leading_steps` to an empty list or `None` (no voice-leading logic exists yet in Phase 2; do not fabricate data — Phase 2.5 concern). Construct the `LoopVariant` with `id` (e.g. `f"{preset.name}-{resolved_seed}"` or `uuid4()` — your discretion), `preset_name=preset.name`, `label` (e.g. `preset.feel` or a short descriptive string), `musicxml_path=None`, `midi_path=None`, `svg_bytes=None`, `midi_bytes=None`, `theory_explanation=None` (Phase 3 concern), and the populated `trace`.

    In `core/presets/registry.py`, add `list_solo_presets() -> list[str]` returning `sorted(n for n in MOOD_PRESETS if MOOD_PRESETS[n].bars)` (WR-01 fix — filters out the 3 duet-only presets whose `bars == []`). Keep `get_preset`/`list_presets` unchanged. Add a matching test in `tests/test_presets_registry.py` asserting `list_solo_presets() == sorted(SOLO_MOODS)` (module already defines `SOLO_MOODS`).
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_loop_engine.py tests/test_presets_registry.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_loop_engine.py -v` passes with zero failures covering every case in `<behavior>`
    - `pytest tests/test_presets_registry.py -v` passes including the new `list_solo_presets` test
    - `grep -c "import argparse\|import streamlit" core/engine/loop_engine.py` returns 0
    - `grep "def build_score" core/engine/loop_engine.py` and `grep "def generate_variant" core/engine/loop_engine.py` both match
    - `pytest tests/test_import_boundary.py -v` still passes (no forbidden imports introduced)
  </acceptance_criteria>
  <done>core/engine/loop_engine.py exposes build_score() and generate_variant() with seed plumbing, full trace population, legacy-note skip, and SAFE-02/SAFE-07 guards; core/presets/registry.py exposes list_solo_presets(); all new and existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build core/export/exporter.py (ExportEngine) with unit tests</name>
  <files>core/export/__init__.py, core/export/exporter.py, tests/test_exporter.py</files>
  <read_first>
    - scripts/generate_cello_dark_ostinato.py (current `export_score` lines 47-60 — exact source of the extraction)
    - .planning/phases/02-loopengine-exportengine/02-PATTERNS.md (lines 159-197: ExportEngine class shape, D-08/D-09/D-10 details, PROJECT_ROOT depth correction for the new file location)
    - .planning/research/ARCHITECTURE.md (line 469: naming guidance `export_to_musicxml()` + `export_to_midi()` vs combined `export()`)
  </read_first>
  <action>
    Create `core/export/__init__.py` (empty file, makes `core.export` a package). Create `core/export/exporter.py` with `from __future__ import annotations` and imports limited to `pathlib.Path`, `music21.midi`, and `music21.stream` (for type hints) — no `argparse`, no `sys.path` bootstrap.

    Implement `class ExportEngine` with `__init__(self, base_dir: Path | None = None) -> None` defaulting `base_dir` to `PROJECT_ROOT / "scores"` where `PROJECT_ROOT` is computed as `Path(__file__).resolve().parents[2]` (this file is at `core/export/exporter.py`, so `parents[2]` reaches the project root — NOT `parents[1]` as used in `scripts/`, per D-08's explicit warning about the different file depth).

    Implement both `export_to_musicxml(self, score: stream.Score, output_name: str) -> Path` and `export_to_midi(self, score: stream.Score, output_name: str) -> Path` as separate methods per ARCHITECTURE.md's naming guidance (line 469), each creating its target subdirectory (`musicxml/` or `midi/`) via `mkdir(parents=True, exist_ok=True)` and writing unconditionally (D-10: no overwrite guard, matches existing behavior). `export_to_musicxml` calls `score.write("musicxml", fp=str(path))`. `export_to_midi` uses `music21.midi.translate.streamToMidiFile(score)` then `.open(path, "wb")` / `.write()` / `.close()` — exact sequence from the source `export_score` function. Also add a convenience method `export(self, score: stream.Score, output_name: str) -> tuple[Path, Path]` that calls both and returns `(musicxml_path, midi_path)` (D-09: "Export returns paths of written files") — this matches the two-return-value call site used by the existing wrapper scripts and simplifies Task 3's refactor.

    Do not read `svg_bytes`/`midi_bytes` back into memory (D-09: these `LoopVariant` fields stay `None` this phase — ExportEngine only writes to disk).
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_exporter.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_exporter.py -v` passes with zero failures
    - Tests use pytest's built-in `tmp_path` fixture to construct `ExportEngine(base_dir=tmp_path)` and assert the written `.musicxml`/`.mid` files exist at `tmp_path/musicxml/{name}.musicxml` and `tmp_path/midi/{name}.mid` — no test writes into the real `scores/` directory
    - `grep "class ExportEngine" core/export/exporter.py` matches
    - `grep "def export_to_musicxml\|def export_to_midi\|def export" core/export/exporter.py` matches all three
  </acceptance_criteria>
  <done>core/export/exporter.py exposes ExportEngine with export_to_musicxml, export_to_midi, and a combined export() method, all writing to a configurable base_dir; unit tests prove file-write behavior using tmp_path.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor scripts/generate_cello_dark_ostinato.py into a thin wrapper; apply WR-01/WR-04 fixes; verify golden regression stays green</name>
  <files>scripts/generate_cello_dark_ostinato.py</files>
  <read_first>
    - scripts/generate_cello_dark_ostinato.py (current full file — the file being rewritten in place)
    - core/engine/loop_engine.py (Task 1's output — exact function signatures: build_score(preset, seed=None))
    - core/export/exporter.py (Task 2's output — exact method signatures)
    - core/presets/registry.py (list_solo_presets — WR-01 fix consumer)
    - .planning/phases/02-loopengine-exportengine/02-PATTERNS.md (lines 282-331: exact target thin-wrapper shape including the `--seed` flag and WR-04's `environment.Environment()` fix)
    - tests/test_golden_regression.py (INVOCATIONS list — the 4 `--genre` invocations this script must still satisfy byte-identically)
  </read_first>
  <action>
    Rewrite `scripts/generate_cello_dark_ostinato.py` to keep `PROJECT_ROOT = Path(__file__).resolve().parents[1]` and the `sys.path.insert` bootstrap (scripts/ keeps this per D-12), but replace the body: remove `build_cello_ostinato` and `export_score` function definitions entirely (their logic now lives in core/), import `from core.engine.loop_engine import build_score`, `from core.export.exporter import ExportEngine`, `from core.presets.registry import get_preset, list_solo_presets` (WR-01: use `list_solo_presets` instead of `list_presets` for the `--genre` choices — replaces the current `list_presets()` call at line 65/15 so duet-only presets are no longer offered).

    Update `parse_args()`: change `choices=sorted(list_presets())` to `choices=sorted(list_solo_presets())`. Add `parser.add_argument("--seed", type=int, default=None)` (D-03: optional seed flag, default `None` preserves existing golden-test behavior since the golden suite never passes `--seed`).

    Update `main()`: replace `environment.UserSettings()["warnings"] = 0` with `environment.Environment()["warnings"] = 0` (WR-04 fix — in-memory only, stops rewriting `~/.music21rc`). Replace the `--list-genres` loop's source from `[get_preset(n) for n in list_presets()]` to `[get_preset(n) for n in list_solo_presets()]` (keeps `--list-genres` consistent with the WR-01 fix — duet presets should not appear here either, since they have empty `feel`). Replace the `build_cello_ostinato(preset)` call with `build_score(preset, seed=args.seed)`. Replace the `export_score(score, output_name)` call with `ExportEngine().export(score, output_name)` (uses the combined method from Task 2, returns `(musicxml_path, midi_path)` matching the existing print statements' variable names — no change needed to the print lines).

    Remove the now-unused imports (`clef, duration, instrument, key, meter, midi, note, stream, tempo` from `music21`, and `MoodPreset` from `core.models`) — the wrapper no longer needs any music21 score-building imports, only `argparse`, `sys`, `pathlib.Path`, and `from music21 import environment`.

    After the rewrite, run the golden regression suite to confirm byte-identical output for the 4 solo-genre invocations this script produces (`cello_dark_trip_hop_ostinato`, `cello_ritual_tribal_ostinato`, `cello_noir_slow_burn_ostinato`, `cello_driving_cinematic_ostinato`). If the hashes differ, diagnose whether the difference is the expected WR-04 side-effect-removal (should have zero effect on file bytes, only on `~/.music21rc`) or an unintended behavior change in the extraction, and fix `core/engine/loop_engine.py`/`core/export/exporter.py` accordingly — do not alter the golden baseline file to "fix" an unexplained mismatch.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_golden_regression.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_golden_regression.py -v` passes with zero failures (both `test_midi_outputs_match_baseline` and `test_musicxml_outputs_match_baseline_normalized`)
    - `python3 scripts/generate_cello_dark_ostinato.py --genre dark_trip_hop` runs with exit code 0 and prints `Genre:`, `MusicXML:`, `MIDI:` lines
    - `python3 scripts/generate_cello_dark_ostinato.py --list-genres` output contains only the 4 solo genre names, no duet preset names
    - `python3 scripts/generate_cello_dark_ostinato.py --genre sexy_duet` fails with a non-zero exit (argparse rejects the choice) — proves WR-01 fix
    - `python3 scripts/generate_cello_dark_ostinato.py --genre dark_trip_hop --seed 42` runs successfully (proves D-03's `--seed` flag is wired end to end)
    - `grep -c "^import\|^from" scripts/generate_cello_dark_ostinato.py` shows no `music21` submodule imports beyond `environment`, and no `core.models` import
    - `grep "environment.UserSettings" scripts/generate_cello_dark_ostinato.py` returns no match (WR-04 fix applied)
  </acceptance_criteria>
  <done>scripts/generate_cello_dark_ostinato.py contains only parse_args()/main() delegating to core.engine.loop_engine.build_score and core.export.exporter.ExportEngine; --genre choices exclude duet presets; --seed flag works; golden regression passes for all 4 solo-genre invocations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> LoopEngine | `--genre`/`--seed`/`--output-name` are user-controlled process args, validated by argparse `choices` and `type=int` before reaching core/ |
| LoopEngine -> filesystem | ExportEngine writes files under `scores/`; base_dir is caller-controlled (Phase 6 will point it elsewhere) |
| Preset data -> Score construction | MoodPreset pitch/rhythm literals flow through validate_pitch/validate_bar_duration (D-06) before becoming music21 notes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | `--genre` CLI arg | mitigate | argparse `choices=sorted(list_solo_presets())` rejects any value outside the known solo-preset set before `get_preset()` is called |
| T-02-05 | Denial of Service | Oversized loop generation (SAFE-02) | mitigate | `build_score`/`generate_variant` raises ValueError before Score construction if bar count exceeds 64 |
| T-02-06 | Denial of Service | Unbounded recursive variant generation (SAFE-07) | mitigate | No self-recursive call path exists in generate_variant/build_score; enforced by code structure, documented with a comment |
| T-02-07 | Tampering | ExportEngine base_dir / output_name path construction | accept | Single-user local CLI tool; output_name derives from a fixed argparse choices set or an explicit --output-name string under the user's own control, not attacker-supplied; low-value target |
| T-02-08 | Repudiation | `~/.music21rc` global config rewrite (WR-04) | mitigate | Switch from `environment.UserSettings()` to `environment.Environment()` — in-memory only, no persistent disk write as a side effect of running the script |
| T-02-SC | Tampering | pip/requirements installs | accept | No new package installs in this plan (music21 already pinned in requirements.txt from Phase 1) |
</threat_model>

<verification>
Run `.venv/bin/python3 -m pytest tests/ -v` — expect zero failures across the full suite (Plan 02-01's validator tests, Plan 02-02's new loop_engine/exporter/registry tests, and the golden regression guard for the 4 solo-genre script invocations).

Manually confirm: `python3 scripts/generate_cello_dark_ostinato.py --genre dark_trip_hop --seed 7` followed by a second identical invocation produces byte-identical MIDI output (seed reproducibility, ROADMAP success criterion 4).
</verification>

<success_criteria>
- `build_score()` returns a valid music21 Score with no errors for any solo MoodPreset.
- `ExportEngine.export_to_musicxml()`/`.export_to_midi()` write valid files to `scores/musicxml/` and `scores/midi/`.
- `scripts/generate_cello_dark_ostinato.py` is a thin wrapper (parse_args + core calls + printing only) producing byte-identical output to before the refactor.
- An explicit seed reproduces an identical variant; an unspecified seed is generated and always recorded in the trace.
- Every generated variant's GenerationTrace has populated pattern_strategy, register_choices, and chord_tones_used.
</success_criteria>

<output>
Create `.planning/phases/02-loopengine-exportengine/02-02-SUMMARY.md` when done
</output>
