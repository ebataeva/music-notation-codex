---
phase: 02-loopengine-exportengine
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - core/engine/loop_engine.py
  - tests/test_loop_engine.py
  - scripts/generate_sexy_duet_loop.py
  - scripts/generate_simple_sexy_duet_loop.py
  - scripts/generate_dorian_sexy_duet_loop.py
autonomous: true
requirements: [LOOP-01]

must_haves:
  truths:
    - "All 4 generator scripts (ostinato + 3 duet) are thin wrappers with no music21 score-building logic remaining in scripts/"
    - "All 3 duet scripts run via their thin wrappers and produce identical output to before the refactor (ROADMAP success criterion 3, extended to duets)"
    - "The internal two-part duet build path is not exposed as public engine API consumed outside scripts/ (D-13 — build_duet_score stays internal, generate_variant stays cello-only)"
    - "The known simple_sexy_duet A1 legacy note survives byte-identically without raising a validation error"
    - "The full test suite including golden regression passes for all 7 script invocations (4 solo + 3 duet)"
  artifacts:
    - path: "core/engine/loop_engine.py"
      provides: "build_duet_score(preset, tempo_bpm, cello_velocity, violin_velocity) -> music21 Score (internal two-part path, D-13)"
      contains: "def build_duet_score"
    - path: "scripts/generate_sexy_duet_loop.py"
      provides: "Thin wrapper delegating to build_duet_score + ExportEngine"
    - path: "scripts/generate_simple_sexy_duet_loop.py"
      provides: "Thin wrapper delegating to build_duet_score + ExportEngine"
    - path: "scripts/generate_dorian_sexy_duet_loop.py"
      provides: "Thin wrapper delegating to build_duet_score + ExportEngine"
  key_links:
    - from: "scripts/generate_sexy_duet_loop.py"
      to: "core/engine/loop_engine.py"
      via: "import build_duet_score"
      pattern: "from core\\.engine\\.loop_engine import build_duet_score"
    - from: "core/engine/loop_engine.py"
      to: "core/engine/validators.py"
      via: "legacy exception skip for simple_sexy_duet A1 note"
      pattern: "_LEGACY_PITCH_EXCEPTIONS"
---

<objective>
Extract the two-part (cello+violin) score-assembly logic shared by the three duet generator scripts into an internal-only `build_duet_score()` function in `core/engine/loop_engine.py` (D-13), refactor all three duet scripts into thin wrappers (D-11/D-12), apply the WR-04 fix to their `main()` functions, and confirm the phase-wide golden regression suite (all 7 script invocations) stays green — completing the phase's full extraction scope.

Purpose: Closes out D-11's requirement that ALL FOUR generator scripts (not just the ostinato one) become thin wrappers in this phase, using the legacy-exception mechanism from Plan 02-02 to let the known `simple_sexy_duet` "A1" out-of-range note survive byte-identically.
Output: `core/engine/loop_engine.py` extended with `build_duet_score`, three refactored thin-wrapper scripts, full golden regression green across all 7 invocations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-loopengine-exportengine/02-CONTEXT.md
@.planning/phases/02-loopengine-exportengine/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add build_duet_score to core/engine/loop_engine.py (internal-only path, D-13) with unit tests including the A1 legacy exception</name>
  <files>core/engine/loop_engine.py, tests/test_loop_engine.py</files>
  <read_first>
    - core/engine/loop_engine.py (Plan 02-02's output — existing build_score, _resolve_seed, _LEGACY_PITCH_EXCEPTIONS, validator wiring — extend this file, do not restructure existing functions)
    - scripts/generate_sexy_duet_loop.py (current full file — `make_note`, `add_measure`, `build_score` lines 20-68 — the exact extraction source; note this script's local `build_score()` name collides with the new module-level `core/engine/loop_engine.py` — rename the duet builder to `build_duet_score` to avoid the clash, per D-13)
    - core/presets/mood_presets.py (duet preset shapes: `duet_rhythm`, `duet_bars`, `duet_tempo_bpm` fields on `sexy_duet`/`simple_sexy_duet`/`dorian_sexy_duet` — confirm `simple_sexy_duet.duet_bars["cello"][1] == ["A1", "E2", "G2", "C#3"]` is the only out-of-range data point)
    - .planning/phases/02-loopengine-exportengine/02-PATTERNS.md (lines 91-137: exact build_duet_score extraction pattern, byte-identity note about tempo/velocity values that must reproduce exactly: sexy_duet 76bpm/82/70, simple_sexy_duet 64bpm/68/58, dorian_sexy_duet 88bpm/74/62 — velocities passed as call-site arguments per the "minimal-risk choice" guidance, not stored on MoodPreset)
  </read_first>
  <behavior>
    - Test: `build_duet_score(get_preset("sexy_duet"), tempo_bpm=76, cello_velocity=82, violin_velocity=70)` returns a `music21.stream.Score` with no exception raised
    - Test: the returned Score has 2 parts (violin, cello) with part ids `"violin"` and `"cello"`
    - Test: the cello part has `len(preset.duet_bars["cello"])` measures; the violin part has `len(preset.duet_bars["violin"])` measures
    - Test: `build_duet_score(get_preset("simple_sexy_duet"), tempo_bpm=64, cello_velocity=68, violin_velocity=58)` does NOT raise despite `duet_bars["cello"][1]` containing the out-of-range `"A1"` note — proves the D-07 legacy exception mechanism from Plan 02-02 is consulted for the duet path too, keyed by preset name `"simple_sexy_duet"`
    - Test: `build_duet_score(get_preset("dorian_sexy_duet"), tempo_bpm=88, cello_velocity=74, violin_velocity=62)` returns a Score with no exception raised
    - Test: `build_duet_score` output is NOT reachable from `generate_variant()` — i.e. `generate_variant` has no `instrument_set`/`duet` parameter (D-13: "generate_variant() stays cello-only"); assert via `inspect.signature(generate_variant)` not containing a duet-related parameter, or simply by code review comment — pick the lighter-weight check
  </behavior>
  <action>
    In `core/engine/loop_engine.py`, add `make_note(pitch_name: str, quarter_length: float, velocity: int) -> note.Note` and `add_measure(part: stream.Part, number: int, pitches: list[str], rhythm: list[float], velocity: int) -> None` as module-level helpers (extracted verbatim in structure from the 3 duet scripts' identical functions), then `build_duet_score(preset: MoodPreset, tempo_bpm: int, cello_velocity: int, violin_velocity: int) -> stream.Score` that builds a two-part Score: `violin` part with `instrument.Violin()` + `clef.TrebleClef()`, `cello` part with `instrument.Violoncello()` + `clef.BassClef()`, both parts getting `tempo.MetronomeMark(number=tempo_bpm)` + `key.Key(preset.key_tonic, preset.key_mode)` + `meter.TimeSignature(preset.meter_signature)`. Read `cello_rhythm = preset.duet_rhythm["cello"]`, `violin_rhythm = preset.duet_rhythm["violin"]`, `cello_bars = preset.duet_bars["cello"]`, `violin_bars = preset.duet_bars["violin"]`, then call `add_measure` per bar for each part with the respective velocity argument. `score.insert(0, violin)` then `score.insert(0, cello)` (preserve insertion order from source scripts — affects part ordering in output, byte-identity-sensitive).

    Before constructing each note inside `add_measure` (or by passing a `preset_name` through to `make_note`/checking before calling it), apply the SAME legacy-exception check used in `build_score` (`_LEGACY_PITCH_EXCEPTIONS` dict from Plan 02-02, already keyed `"simple_sexy_duet": {"A1"}`) — skip `validate_pitch` for excepted `(preset_name, pitch_name)` pairs and emit a warning, exactly as done for the solo path. Call `validate_bar_duration` for both `cello_rhythm`/preset.meter_signature and `violin_rhythm`/preset.meter_signature once each per preset.

    Do NOT expose `build_duet_score` through `generate_variant()` or any other public "variant" API — this function is called directly by the three duet scripts only, per D-13 ("internal path only... NOT exposed as public engine API. generate_variant() stays cello-only"). Add a code comment at the top of `build_duet_score` stating this scoping explicitly (e.g. "Internal-only two-part path (D-13) — not exposed via generate_variant(); DUET-01 public duet API is v2 scope.").
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_loop_engine.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_loop_engine.py -v` passes with zero failures including all new duet-path test cases
    - `grep "def build_duet_score" core/engine/loop_engine.py` matches
    - `grep -c "def generate_variant" core/engine/loop_engine.py` returns exactly 1, and its signature (checked via `grep -A3 "def generate_variant"`) contains no `instrument_set`/`duet` parameter
    - The `simple_sexy_duet` A1-note test in `<behavior>` passes without raising
  </acceptance_criteria>
  <done>core/engine/loop_engine.py exposes build_duet_score() as an internal two-part builder reusing the D-07 legacy exception mechanism; generate_variant() remains cello-only and unmodified; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all 3 duet scripts into thin wrappers; apply WR-04 fix; verify full 7-invocation golden regression</name>
  <files>scripts/generate_sexy_duet_loop.py, scripts/generate_simple_sexy_duet_loop.py, scripts/generate_dorian_sexy_duet_loop.py</files>
  <read_first>
    - scripts/generate_sexy_duet_loop.py (current full file — being rewritten in place; template for the other two per 02-PATTERNS.md's note that all 3 share byte-identical structure differing only in preset name/tempo/velocity constants)
    - scripts/generate_simple_sexy_duet_loop.py (current full file — velocities 68/58, tempo 64, preset "simple_sexy_duet")
    - scripts/generate_dorian_sexy_duet_loop.py (current full file — velocities 74/62, tempo 88, preset "dorian_sexy_duet")
    - core/engine/loop_engine.py (Task 1's output — build_duet_score exact signature: `build_duet_score(preset, tempo_bpm, cello_velocity, violin_velocity)`)
    - core/export/exporter.py (Plan 02-02's output — ExportEngine().export() combined method)
    - .planning/phases/02-loopengine-exportengine/02-PATTERNS.md (lines 339-372: exact target thin-wrapper shape for the duet scripts, byte-identity risk note about keeping velocities/tempo as call-site literals in the wrapper, not moved into MoodPreset)
    - tests/test_golden_regression.py (INVOCATIONS list — the 3 duet invocations: `sexy_d_minor_violin_cello_loop`, `simple_sexy_d_minor_violin_cello_loop`, `dorian_sexy_violin_cello_loop`, each with no CLI args)
  </read_first>
  <action>
    For each of the 3 duet scripts, rewrite to keep `PROJECT_ROOT = Path(__file__).resolve().parents[1]` + `sys.path.insert` bootstrap + `OUTPUT_NAME`/`MUSICXML_PATH`/`MIDI_PATH` module constants (D-12: thin wrapper scope, these are wrapper-level path concerns) unchanged, but remove `make_note`, `add_measure`, and `build_score` (the local one) function definitions entirely. Import `from core.engine.loop_engine import build_duet_score` and `from core.export.exporter import ExportEngine` and `from core.presets.registry import get_preset`.

    In each script's `main()`: replace `environment.UserSettings()["warnings"] = 0` with `environment.Environment()["warnings"] = 0` (WR-04 fix, same as Plan 02-02 Task 3). Replace the local `build_score()` call with `build_duet_score(get_preset(PRESET_NAME), tempo_bpm=TEMPO, cello_velocity=CELLO_VEL, violin_velocity=VIOLIN_VEL)` using each script's exact existing literals: `generate_sexy_duet_loop.py` uses preset `"sexy_duet"`, tempo `76`, cello velocity `82`, violin velocity `70`; `generate_simple_sexy_duet_loop.py` uses preset `"simple_sexy_duet"`, tempo `64`, cello velocity `68`, violin velocity `58`; `generate_dorian_sexy_duet_loop.py` uses preset `"dorian_sexy_duet"`, tempo `88`, cello velocity `74`, violin velocity `62`. Replace the local `export_score(score)` call with `ExportEngine().export(score, OUTPUT_NAME)`, which returns `(musicxml_path, midi_path)` — update each script's `main()` print statements to print this returned tuple (consistent with the ostinato wrapper from Plan 02-02) rather than the old module-level `MUSICXML_PATH`/`MIDI_PATH` constants; the constants may stay defined for `OUTPUT_NAME` derivation but the printed paths must reflect what `ExportEngine` actually wrote.

    Remove now-unused imports (`clef, duration, instrument, key, meter, midi, note, stream, tempo` from `music21`) from all 3 scripts — each keeps only `from music21 import environment`, plus `sys`, `pathlib.Path` (these scripts never had argparse and still don't need it).

    After all 3 rewrites, run the full golden regression suite (all 7 invocations: 4 solo + 3 duet) to confirm byte-identical output. Pay special attention to `simple_sexy_d_minor_violin_cello_loop` since it is the one invocation touching the `"A1"` legacy note through the newly validator-wired path — if this specific hash fails while others pass, the legacy exception mechanism (Task 1) is the most likely culprit. Do not alter the golden baseline file to "fix" an unexplained mismatch; diagnose and fix the extraction instead.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_golden_regression.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_golden_regression.py -v` passes with zero failures across all 7 invocations (both `test_midi_outputs_match_baseline` and `test_musicxml_outputs_match_baseline_normalized`)
    - `python3 scripts/generate_sexy_duet_loop.py` runs with exit code 0 and prints `MusicXML:`/`MIDI:` lines matching files that actually exist on disk
    - `python3 scripts/generate_simple_sexy_duet_loop.py` runs with exit code 0 (proves the A1 legacy note does not crash the run end to end)
    - `python3 scripts/generate_dorian_sexy_duet_loop.py` runs with exit code 0
    - `grep -c "environment.UserSettings" scripts/generate_sexy_duet_loop.py scripts/generate_simple_sexy_duet_loop.py scripts/generate_dorian_sexy_duet_loop.py` returns 0 total matches (WR-04 fix applied to all 3)
    - `grep -L "def make_note\|def add_measure\|def build_score" scripts/generate_sexy_duet_loop.py scripts/generate_simple_sexy_duet_loop.py scripts/generate_dorian_sexy_duet_loop.py` lists all 3 files (proves no local score-building functions remain)
  </acceptance_criteria>
  <done>All 3 duet scripts are thin wrappers delegating to core.engine.loop_engine.build_duet_score and core.export.exporter.ExportEngine, with the WR-04 environment fix applied; full 7-invocation golden regression suite passes with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Duet preset data -> build_duet_score | `core/presets/mood_presets.py` duet_bars/duet_rhythm literals (including the known out-of-range A1 note) flow through the same validator + legacy-exception path as the solo builder |
| build_duet_score -> ExportEngine | Two-part Score objects flow to the same file-write path as solo scores, no additional trust boundary beyond Plan 02-02's |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | Legacy exception table scope creep | mitigate | `_LEGACY_PITCH_EXCEPTIONS` remains keyed strictly by `(preset_name, pitch_name)` pairs; the duet path reuses the identical table from Plan 02-02 rather than introducing a separate, looser bypass mechanism |
| T-02-10 | Elevation of Privilege | `build_duet_score` becoming reachable from public variant-generation API | mitigate | `generate_variant()` signature unchanged in this plan (verified by acceptance criteria grep); `build_duet_score` is called only from the 3 duet scripts |
| T-02-SC | Tampering | pip/requirements installs | accept | No new package installs in this plan (music21 already pinned in requirements.txt from Phase 1) |
</threat_model>

<verification>
Run `.venv/bin/python3 -m pytest tests/ -v` — expect zero failures across the entire suite: Plan 02-01's validator tests, Plan 02-02's loop_engine/exporter/registry tests, this plan's duet-path tests, and the golden regression guard covering all 7 script invocations (4 solo + 3 duet).
</verification>

<success_criteria>
- All 4 generator scripts are thin wrappers (parse_args/module constants + core calls + printing only).
- The 3 duet scripts produce byte-identical output to before the refactor.
- build_duet_score exists as an internal-only path, not reachable via generate_variant().
- The simple_sexy_duet A1 legacy note survives without raising a validation error.
- Full golden regression suite (7 invocations) passes with zero failures.
</success_criteria>

<output>
Create `.planning/phases/02-loopengine-exportengine/02-03-SUMMARY.md` when done
</output>
