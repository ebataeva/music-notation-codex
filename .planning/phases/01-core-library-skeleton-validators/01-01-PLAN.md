---
phase: 01-core-library-skeleton-validators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/__init__.py
  - core/models.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_models.py
  - tests/test_import_boundary.py
  - pytest.ini
autonomous: true
requirements: [PLAT-03]

must_haves:
  truths:
    - "All five canonical dataclasses (MoodPreset, GenerationRequest, LoopVariant, TheoryExplanation, GenerationTrace) exist as importable Python types in core/models.py"
    - "GenerationTrace has exactly the five locked fields (seed, pattern_strategy, register_choices, voice_leading_steps, chord_tones_used) so Phase 2/2.5 can populate them without a schema change"
    - "LoopVariant.trace is optional (defaults to None) and no field on any dataclass requires a live music21 object to construct"
    - "pytest tests/ can discover and run tests scoped only to tests/, never touching a future tests-ui/ directory"
    - "core/ contains zero imports of streamlit or argparse, enforced by an automated test"
  artifacts:
    - path: "core/models.py"
      provides: "MoodPreset, GenerationRequest, LoopVariant, TheoryExplanation, GenerationTrace dataclasses"
      contains: "class GenerationTrace"
    - path: "tests/test_models.py"
      provides: "Unit tests asserting dataclass shape and GenerationTrace field completeness"
      exports: ["test_generation_trace_fields"]
    - path: "tests/test_import_boundary.py"
      provides: "AST-based static check that core/ never imports streamlit or argparse"
    - path: "pytest.ini"
      provides: "testpaths scoped to tests/ only"
      contains: "testpaths"
  key_links:
    - from: "tests/test_models.py"
      to: "core/models.py"
      via: "from core.models import ..."
      pattern: "from core\\.models import"
    - from: "tests/test_import_boundary.py"
      to: "core/"
      via: "ast.parse of every core/**/*.py file"
      pattern: "core_root = Path"
---

<objective>
Create the pure-Python `core/models.py` dataclass module (all five canonical shapes, including the new `GenerationTrace`) and stand up the first pytest infrastructure for this repo (`tests/`, `pytest.ini`), with unit tests proving the dataclasses' shape and an import-boundary guard proving `core/` never imports `streamlit` or `argparse`.

Purpose: Every later phase (2, 2.5, 3, and the Streamlit UI in 4+) constructs and passes these five dataclasses between layers. Getting the shape right now — especially `GenerationTrace`'s locked fields and `LoopVariant`'s serialization-friendly optional fields — avoids retrofitting a schema change into code that Phase 2+ will already be building against. This plan also produces the only pytest scaffold in the repo; all subsequent Phase 1 plans (validators, golden regression, preset registry) write test files into the `tests/` directory this plan establishes.
Output: `core/__init__.py`, `core/models.py`, `pytest.ini`, `tests/__init__.py`, `tests/conftest.py`, `tests/test_models.py`, `tests/test_import_boundary.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-library-skeleton-validators/01-CONTEXT.md
@.planning/phases/01-core-library-skeleton-validators/01-RESEARCH.md
@.planning/phases/01-core-library-skeleton-validators/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create core/models.py with the five canonical dataclasses</name>
  <files>core/__init__.py, core/models.py, tests/__init__.py, tests/conftest.py, tests/test_models.py, pytest.ini</files>
  <read_first>
    - scripts/generate_cello_dark_ostinato.py (lines 1-23: existing `GenrePreset` frozen dataclass — the only prior dataclass pattern in this codebase; note `from __future__ import annotations` at top, `@dataclass(frozen=True)`, plain `list[float]`/`list[list[str]]` hints)
    - .planning/phases/01-core-library-skeleton-validators/01-PATTERNS.md (lines 66-125: exact canonical target shapes for all five dataclasses, cross-checked against CONTEXT.md)
    - .planning/phases/01-core-library-skeleton-validators/01-CONTEXT.md (lines 17-21: GenerationTrace field lock; line 21: serialization-friendliness requirement — music21 objects must be separable from persistable payload)
  </read_first>
  <behavior>
    - Test 1: `from core.models import MoodPreset, GenerationRequest, LoopVariant, TheoryExplanation, GenerationTrace` succeeds with no ImportError.
    - Test 2: `GenerationTrace` constructed with `seed=42, pattern_strategy="ostinato", register_choices=["low"], voice_leading_steps=["C2->G2"], chord_tones_used=[["C","Eb","G"]]` exposes exactly those five field names (verified via `dataclasses.fields(GenerationTrace)` name set equals `{"seed", "pattern_strategy", "register_choices", "voice_leading_steps", "chord_tones_used"}`).
    - Test 3: `LoopVariant` constructed WITHOUT passing `trace` defaults `trace` to `None` (optional field, per CONTEXT.md "LoopVariant carries an optional trace: GenerationTrace | None field from day one").
    - Test 4: `MoodPreset` constructed with all of PATTERNS.md's listed fields (including `duet_rhythm=None, duet_bars=None, duet_tempo_bpm=None` defaults) succeeds; `MoodPreset` is frozen (attempting attribute mutation after construction raises `dataclasses.FrozenInstanceError`).
    - Test 5: `GenerationRequest` constructed with only `chord_progression, key_tonic, key_mode, mood` (using defaults for `num_variants=3, bars=8, instrument_set="cello"`) succeeds and exposes those default values.
  </behavior>
  <action>
    Create `core/__init__.py` as an empty package marker. Create `core/models.py` with `from __future__ import annotations` at the top and exactly five dataclasses matching 01-PATTERNS.md lines 66-125: `GenerationRequest` (plain `@dataclass`, fields `chord_progression: str, key_tonic: str, key_mode: str, mood: str, num_variants: int = 3, bars: int = 8, instrument_set: str = "cello"`), `TheoryExplanation` (plain `@dataclass`, fields `why_it_works, how_to_start, how_to_develop, how_to_end, how_to_transition` all `str`), `GenerationTrace` (plain `@dataclass`, fields `seed: int | None, pattern_strategy: str | None, register_choices: list[str] | None, voice_leading_steps: list[str] | None, chord_tones_used: list[list[str]] | None`), `LoopVariant` (plain `@dataclass`, fields `id: str, preset_name: str, label: str, musicxml_path: Path | None, midi_path: Path | None, svg_bytes: bytes | None, midi_bytes: bytes | None, theory_explanation: TheoryExplanation | None, trace: GenerationTrace | None = None` — import `Path` from `pathlib`; deliberately no `score: music21.stream.Score` field, matching CONTEXT.md's serialization-friendliness rule), `MoodPreset` (`@dataclass(frozen=True)`, fields `name: str, tempo_bpm: int, key_tonic: str, key_mode: str, meter_signature: str, velocity: int, rhythm: list[float], bars: list[list[str]], feel: str, progressions: list[str], modulations: list[str], mood_tips: list[str], duet_rhythm: dict[str, list[float]] | None = None, duet_bars: dict[str, list[list[str]]] | None = None, duet_tempo_bpm: int | None = None`). Add one module docstring line only if a design choice is non-obvious (e.g. why `trace` defaults to `None`); do not add comments restating field names. Create `pytest.ini` at repo root with a `[pytest]` section setting `testpaths = tests` (scopes discovery away from the not-yet-existing `tests-ui/`, per TEST-01 boundary and RESEARCH.md Validation Architecture). Create `tests/__init__.py` (empty). Create `tests/conftest.py` with a `sys.path` bootstrap only if needed for `from core.models import ...` to resolve from `tests/` — verify first whether `pytest.ini` at repo root + running from repo root already makes `core` importable via rootdir-relative import (music21's own scripts import via `from pathlib import Path; PROJECT_ROOT = Path(__file__).resolve().parents[1]`-style paths, but pytest's rootdir insertion typically suffices); if a fixture is genuinely needed, add a `tolerance` float fixture (e.g. `1e-9`) for later plans' float-comparison tests, since RESEARCH.md's Wave 0 gap list anticipates this. Create `tests/test_models.py` implementing the five behaviors above as five separate test functions (not one mega-test).
  </action>
  <acceptance_criteria>
    - `from core.models import MoodPreset, GenerationRequest, LoopVariant, TheoryExplanation, GenerationTrace` succeeds when run as `.venv/bin/python3 -c "..."` from repo root
    - `python3 -c "import dataclasses, core.models as m; print(sorted(f.name for f in dataclasses.fields(m.GenerationTrace)))"` prints exactly `['chord_tones_used', 'pattern_strategy', 'register_choices', 'seed', 'voice_leading_steps']`
    - `grep -c "class MoodPreset" core/models.py` equals 1, and the class is decorated `@dataclass(frozen=True)` (grep the line immediately preceding the class definition)
    - `core/models.py` contains no `import streamlit` and no `import argparse` (grep confirms zero matches)
    - `.venv/bin/python3 -m pytest tests/test_models.py -v` shows 5 passed, 0 failed
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_models.py -v</automated>
  </verify>
  <done>core/models.py exists with all five dataclasses matching the locked shapes; tests/test_models.py passes 5/5; pytest.ini scopes discovery to tests/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add import-boundary guard test for core/</name>
  <files>tests/test_import_boundary.py</files>
  <read_first>
    - core/models.py (the file created by Task 1 — read its final imports to confirm the guard test will pass against it)
    - .planning/phases/01-core-library-skeleton-validators/01-PATTERNS.md (lines 288-308: reference AST-based import-boundary test implementation)
    - .planning/research/ARCHITECTURE.md (line 122: "No Streamlit import may appear below the app/ layer... The boundary is enforced at the import level")
  </read_first>
  <behavior>
    - Test 1: Walking every `.py` file under `core/` with `ast.parse` and `ast.walk`, no `Import` or `ImportFrom` node has a top-level module name of `streamlit` or `argparse`.
    - Test 2 (regression guard): temporarily constructing an in-memory AST for a string like `"import streamlit"` and asserting the detection logic itself would flag it (a unit test of the checker logic, not just an integration sweep) — OR, if simpler, assert the sweep function returns a non-empty violation set when pointed at a fixture file containing a forbidden import, proving the checker isn't vacuously passing because it silently skips all files.
  </behavior>
  <action>
    Create `tests/test_import_boundary.py` implementing the AST-walk pattern from 01-PATTERNS.md lines 288-308 almost verbatim: define `FORBIDDEN = {"streamlit", "argparse"}`, a `core_root = Path(__file__).resolve().parents[1] / "core"` walk over `core_root.rglob("*.py")`, and assert no forbidden top-level module name appears in any `ast.Import`/`ast.ImportFrom` node. Additionally add a second test function that proves the checker is not vacuous: write a small forbidden-import source string to a `tmp_path` fixture file (e.g. `tmp_path / "fake_module.py"` containing `"import argparse\n"`), run the same AST-walk logic against that single file, and assert it DOES detect the violation (this guards against the test silently always passing due to an empty glob or a typo in the forbidden-set check). Keep the detection logic as a small importable helper function (e.g. `_forbidden_imports_in_file(path: Path) -> set[str]`) so both test functions call the same code path instead of duplicating the AST walk.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python3 -m pytest tests/test_import_boundary.py -v` shows 2 passed, 0 failed
    - The vacuity-check test fails if the forbidden-set check is removed (verified by the test's own design: it asserts a positive detection on a known-bad fixture, not just "core/ passes")
    - `grep -rn "import streamlit\|import argparse" core/` returns no matches (confirms the guard's real-world target state)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_import_boundary.py -v</automated>
  </verify>
  <done>tests/test_import_boundary.py passes 2/2, proving both that core/ is currently clean and that the checker would catch a real violation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| core/ ↔ future app/ (Streamlit) | Import-level boundary only in this phase — no runtime data crosses yet; enforced statically |
| Developer-authored dataclass construction | No untrusted external input in this phase; all constructors are called by test code or future in-repo callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-01 | Tampering | core/models.py dataclass fields | accept | No I/O or external input in this plan; dataclasses are pure data shapes constructed only by trusted in-repo code (tests, future core/engine code) — no attacker-controlled path exists yet |
| T-01-02 | Information Disclosure | LoopVariant.musicxml_path / midi_path (Path fields) | accept | Paths are typed as `Path | None`, never resolved or read from disk in this plan; no file I/O occurs until Phase 2's ExportEngine — deferred, not this plan's surface |
| T-01-03 | Tampering | tests/test_import_boundary.py vacuity | mitigate | Task 2 explicitly adds a positive-detection test against a known-bad fixture file, preventing the guard test from silently always passing |
</threat_model>

<verification>
Run `.venv/bin/python3 -m pytest tests/ -v` after both tasks — expect 7 passed (5 from test_models.py + 2 from test_import_boundary.py), 0 failed.
</verification>

<success_criteria>
- `core/models.py` defines exactly the five canonical dataclasses with the CONTEXT.md-locked `GenerationTrace` fields
- `pytest tests/` runs and passes with zero failures, discovery scoped to `tests/` only
- Import-boundary guard proves `core/` has no `streamlit`/`argparse` imports and would catch a regression
</success_criteria>

<output>
Create `.planning/phases/01-core-library-skeleton-validators/01-01-SUMMARY.md` when done
</output>
