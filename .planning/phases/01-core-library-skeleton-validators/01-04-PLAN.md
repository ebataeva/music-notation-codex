---
phase: 01-core-library-skeleton-validators
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - core/presets/__init__.py
  - core/presets/mood_presets.py
  - core/presets/registry.py
  - tests/test_presets_registry.py
  - scripts/generate_cello_dark_ostinato.py
  - scripts/harmony_advisor.py
  - scripts/generate_sexy_duet_loop.py
  - scripts/generate_simple_sexy_duet_loop.py
  - scripts/generate_dorian_sexy_duet_loop.py
autonomous: true
requirements: [LOOP-03, LOOP-04]

must_haves:
  truths:
    - "The MoodPreset registry merges data from all 5 existing scripts (GENRE_PRESETS + GENRE_IDEAS for 4 solo moods, plus 3 duet scripts as 3 separate entries) into 7 total MoodPreset entries"
    - "All existing CLI scripts still produce the same output files after refactoring their data into core/presets/ (Roadmap Phase 1 success criterion 3), verified by the golden regression test from Plan 03 passing after this data move"
    - "The pre-existing out-of-range A1 note in generate_simple_sexy_duet_loop.py cello_bars migrates verbatim (golden no-op) and is flagged with an inline comment, without validators rejecting it in this phase"
    - "core/ never imports argparse (registry.py and mood_presets.py stay import-independent of Streamlit and CLI concerns)"
  artifacts:
    - path: "core/presets/mood_presets.py"
      provides: "MOOD_PRESETS: dict[str, MoodPreset] — 7 entries merged from GENRE_PRESETS + GENRE_IDEAS + 3 duet scripts"
      exports: ["MOOD_PRESETS"]
      contains: "MOOD_PRESETS"
    - path: "core/presets/registry.py"
      provides: "get_preset(name) -> MoodPreset; list_presets() -> list[str]"
      exports: ["get_preset", "list_presets"]
    - path: "tests/test_presets_registry.py"
      provides: "Unit tests proving merge completeness (all 5 sources present, no silent data loss, 7 total entries)"
  key_links:
    - from: "scripts/generate_cello_dark_ostinato.py"
      to: "core/presets/registry.py"
      via: "from core.presets.registry import get_preset, list_presets"
      pattern: "from core\\.presets\\.registry import"
    - from: "core/presets/mood_presets.py"
      to: "core/models.py"
      via: "from core.models import MoodPreset"
      pattern: "from core\\.models import MoodPreset"
    - from: "tests/test_golden_regression.py"
      to: "tests/golden/baseline_hashes.json"
      via: "re-run after this plan's data move, compared against Plan 03's pre-move baseline"
      pattern: "baseline_hashes\\.json"
---

<objective>
Merge `GENRE_PRESETS` (from `generate_cello_dark_ostinato.py`) and `GENRE_IDEAS` (from `harmony_advisor.py`) into one `MOOD_PRESETS: dict[str, MoodPreset]` registry keyed by mood name, add the three duet scripts as three additional standalone `MoodPreset` entries with per-instrument duet fields, swap all 5 scripts' preset data source to import from `core/presets/`, and verify the migration is a byte-for-byte no-op against Plan 03's golden baseline.

Purpose: This is the actual data-extraction step the whole phase exists to enable — moving hardcoded dicts and literals out of `scripts/` into a single source of truth (`core/presets/`) that Phase 2's `LoopEngine` and Phase 3's `TheoryExplainer` will both read from (ARCHITECTURE.md Pattern 2). The scripts must keep working identically; only their preset data source changes (full engine refactor to thin wrappers is Phase 2, per CONTEXT.md). The pre-existing out-of-range `"A1"` note in the simple duet script must migrate verbatim — this plan is data-only, and validators (built in Plan 02) are not yet wired into any generation call path.
Output: `core/presets/mood_presets.py`, `core/presets/registry.py`, 5 modified scripts (data source swap only), `tests/test_presets_registry.py`. Golden regression from Plan 03 re-verified green after the move.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-library-skeleton-validators/01-CONTEXT.md
@.planning/phases/01-core-library-skeleton-validators/01-RESEARCH.md
@.planning/phases/01-core-library-skeleton-validators/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build core/presets/mood_presets.py and registry.py — merge all 5 sources into 7 MoodPreset entries</name>
  <files>core/presets/__init__.py, core/presets/mood_presets.py, core/presets/registry.py, tests/test_presets_registry.py</files>
  <read_first>
    - scripts/generate_cello_dark_ostinato.py (lines 26-107: full GENRE_PRESETS dict — 4 entries: dark_trip_hop, ritual_tribal, noir_slow_burn, driving_cinematic — every field, every bar, every rhythm value, feel string verbatim)
    - scripts/harmony_advisor.py (lines 7-68: full GENRE_IDEAS dict — same 4 keys, each with progressions/modulations/mood lists, verbatim Russian text)
    - scripts/generate_sexy_duet_loop.py (lines 44-69: cello_rhythm 7 elements, violin_rhythm 5 elements, cello_bars, violin_bars, 76 BPM, D minor, velocities cello=82/violin=70)
    - scripts/generate_simple_sexy_duet_loop.py (lines 44-68: shared rhythm=[1.0,1.0,1.0,1.0] for both parts, cello_bars contains "A1" in rows 2/4/6/8, 64 BPM, D minor, velocities cello=68/violin=58)
    - scripts/generate_dorian_sexy_duet_loop.py (lines 44-68: cello_rhythm 7 elements, violin_rhythm 6 elements, cello_bars, violin_bars, 88 BPM, D minor, velocities cello=74/violin=62)
    - core/models.py (from Plan 01-01 — the exact MoodPreset field names/types this data must be shaped into)
    - .planning/phases/01-core-library-skeleton-validators/01-PATTERNS.md (lines 129-224: merge key alignment, duet preset naming recommendation `sexy_duet`/`simple_sexy_duet`/`dorian_sexy_duet`, registry helper pattern)
  </read_first>
  <behavior>
    - Test 1: `from core.presets.mood_presets import MOOD_PRESETS` succeeds; `len(MOOD_PRESETS) == 7`.
    - Test 2: `set(MOOD_PRESETS) == {"dark_trip_hop", "ritual_tribal", "noir_slow_burn", "driving_cinematic", "sexy_duet", "simple_sexy_duet", "dorian_sexy_duet"}`.
    - Test 3: For each of the 4 solo moods, `MOOD_PRESETS[name].progressions` and `.modulations` and `.mood_tips` are non-empty lists sourced from `GENRE_IDEAS[name]` (proves the merge actually pulled theory data in, not just generation data) — spot-check `MOOD_PRESETS["dark_trip_hop"].feel == "темный, сексуальный, петлевой trip-hop groove"` (verbatim migration, Russian text preserved).
    - Test 4: `MOOD_PRESETS["sexy_duet"].duet_rhythm == {"cello": [0.5, 0.5, 1.0, 0.5, 0.5, 0.5, 0.5], "violin": [1.0, 0.5, 0.5, 1.0, 1.0]}` (exact per-instrument shape from the real script, proving Pitfall 3's divergent-length issue is handled correctly).
    - Test 5: `MOOD_PRESETS["simple_sexy_duet"].duet_bars["cello"][1] == ["A1", "E2", "G2", "C#3"]` (proves the pre-existing out-of-range A1 note migrated verbatim, not silently fixed).
    - Test 6: `from core.presets.registry import get_preset, list_presets`; `get_preset("dark_trip_hop").name == "dark_trip_hop"`; `get_preset("nonexistent")` raises `KeyError`; `sorted(list_presets()) == sorted(MOOD_PRESETS)`.
    - Test 7: The 4 solo `MoodPreset` entries have `duet_rhythm is None and duet_bars is None and duet_tempo_bpm is None` (solo presets don't carry duet fields); the 3 duet entries have those three fields populated (non-None).
  </behavior>
  <action>
    Create `core/presets/__init__.py` as an empty package marker. Create `core/presets/mood_presets.py` with `from __future__ import annotations`, importing `MoodPreset` from `core.models`, and building `MOOD_PRESETS: dict[str, MoodPreset] = {...}` with exactly 7 entries: the 4 solo moods built by merging each `GENRE_PRESETS[name]` field set with the matching `GENRE_IDEAS[name]` dict's `progressions`/`modulations`/`mood` (mapped to `mood_tips`) — migrate every bar, rhythm value, and feel/progression/modulation/mood string verbatim (character-for-character, including Russian text) from the two source files read above; plus 3 duet entries (`sexy_duet`, `simple_sexy_duet`, `dorian_sexy_duet`), each a `MoodPreset` with `duet_rhythm: dict[str, list[float]]` and `duet_bars: dict[str, list[list[str]]]` keyed `"cello"`/`"violin"` matching each duet script's actual (possibly divergent-length) rhythm/bar data exactly, plus `duet_tempo_bpm` set to that script's tempo (76/64/88 respectively); for the duet entries' base (non-duet) fields (`rhythm`, `bars`, `tempo_bpm`, `feel`, `progressions`, `modulations`, `mood_tips`), use reasonable empty/placeholder-free values consistent with the schema (e.g. `rhythm=[]`, `bars=[]` since duet scripts have no solo-cello-only equivalent data — do NOT invent solo bar data that doesn't exist in source; `tempo_bpm` should equal `duet_tempo_bpm` for schema consistency; `feel` can carry each duet script's descriptive comment text, e.g. "D Dorian vamp: Dm9 -> G9. The B natural keeps it warm instead of funeral-dark." for dorian_sexy_duet, migrated from the script's own code comment; `progressions`/`modulations`/`mood_tips` as empty lists `[]` since no `GENRE_IDEAS`-equivalent theory data exists for duets — do not fabricate theory text). At the `simple_sexy_duet` entry's `duet_bars["cello"]` data point, add the exact inline comment specified in 01-PATTERNS.md: `# NOTE: "A1" (MIDI 33) is below the C2 validator floor — pre-existing in source script, migrated verbatim, not yet validated at generation time (Phase 2 concern).` Create `core/presets/registry.py` with `get_preset(name: str) -> MoodPreset` (plain `MOOD_PRESETS[name]` lookup, letting the natural `KeyError` propagate — no custom exception, no try/except, matching 01-PATTERNS.md's "keep it simple" guidance) and `list_presets() -> list[str]` (`sorted(MOOD_PRESETS)` — matches the existing `sorted(GENRE_PRESETS)` CLI convention). Create `tests/test_presets_registry.py` implementing the 7 behaviors above as 7 separate test functions.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python3 -m pytest tests/test_presets_registry.py -v` shows 7 passed, 0 failed
    - `python3 -c "from core.presets.mood_presets import MOOD_PRESETS; print(len(MOOD_PRESETS))"` prints `7`
    - `grep -c "A1.*MIDI 33" core/presets/mood_presets.py` is at least 1 (the required inline flag comment is present)
    - `grep -n "import argparse\|import streamlit" core/presets/mood_presets.py core/presets/registry.py` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_presets_registry.py -v</automated>
  </verify>
  <done>core/presets/mood_presets.py and registry.py exist; all 7 MoodPreset entries present with data migrated verbatim from the 5 source scripts; 7/7 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Swap all 5 scripts' preset data source to core/presets/ and verify golden regression stays green</name>
  <files>scripts/generate_cello_dark_ostinato.py, scripts/harmony_advisor.py, scripts/generate_sexy_duet_loop.py, scripts/generate_simple_sexy_duet_loop.py, scripts/generate_dorian_sexy_duet_loop.py</files>
  <read_first>
    - core/presets/registry.py, core/presets/mood_presets.py (the files created in Task 1 — confirm exact import names before swapping)
    - scripts/generate_cello_dark_ostinato.py (current full content — the `GenrePreset` class at lines 13-23 and `GENRE_PRESETS` dict at lines 26-107 must be removed; `parse_args` line 157 uses `sorted(GENRE_PRESETS)`; `main()` lines 167-172 use `GENRE_PRESETS.values()` and `GENRE_PRESETS[args.genre]`)
    - scripts/harmony_advisor.py (current full content — `GENRE_IDEAS` dict at lines 7-68 must be removed; `parse_args` line 81 uses `sorted(GENRE_IDEAS)`; `main()` lines 89-101 use `GENRE_IDEAS[args.genre]` and iterate `sorted(GENRE_IDEAS)`)
    - scripts/generate_sexy_duet_loop.py, scripts/generate_simple_sexy_duet_loop.py, scripts/generate_dorian_sexy_duet_loop.py (each has hardcoded `cello_rhythm`/`violin_rhythm`/`cello_bars`/`violin_bars` module-level literals inside `build_score()`, lines 44-69/44-68/44-68 respectively, that must be replaced by a registry lookup)
    - tests/golden/baseline_hashes.json (Plan 01-03's captured baseline — the exact comparison target for this task's verification)
  </read_first>
  <action>
    In `scripts/generate_cello_dark_ostinato.py`: remove the `GenrePreset` dataclass definition (lines 13-23) and the entire `GENRE_PRESETS` dict literal (lines 26-107); add `from core.presets.registry import get_preset, list_presets` near the top; replace `sorted(GENRE_PRESETS)` (line 157, inside `parse_args`'s `choices=` argument) with `sorted(list_presets())`; replace `GENRE_PRESETS.values()` (line 168) with `[get_preset(n) for n in list_presets()]`; replace `GENRE_PRESETS[args.genre]` (line 172) with `get_preset(args.genre)`. Leave `build_cello_ostinato`, `export_score`, `parse_args`, `main` structurally unchanged otherwise — they operate on the `MoodPreset` object exactly as they did on `GenrePreset` (duck-typed attribute access: `.name`, `.tempo_bpm`, `.key_tonic`, `.key_mode`, `.meter_signature`, `.rhythm`, `.bars`, `.velocity`, `.feel` all still exist on `MoodPreset`). In `scripts/harmony_advisor.py`: remove the `GENRE_IDEAS` dict literal (lines 7-68); add `from core.presets.registry import get_preset, list_presets`; replace `sorted(GENRE_IDEAS)` (line 81, `choices=`) with `sorted(list_presets())`; replace `GENRE_IDEAS[args.genre]` (line 95) with a `MoodPreset` read — `preset = get_preset(args.genre)`, then adapt `ideas["progressions"]`/`ideas["modulations"]`/`ideas["mood"]` (lines 100-102) to `preset.progressions`/`preset.modulations`/`preset.mood_tips`; replace the `for genre in sorted(GENRE_IDEAS):` loop (line 91) with `for genre in sorted(list_presets()):`. In each duet script: add `from core.presets.registry import get_preset` near the top; inside `build_score()`, replace the hardcoded `cello_rhythm`/`violin_rhythm`/`cello_bars`/`violin_bars` literal assignments with `preset = get_preset("sexy_duet")` (or `"simple_sexy_duet"` / `"dorian_sexy_duet"` matching each script), then `cello_rhythm = preset.duet_rhythm["cello"]`, `violin_rhythm = preset.duet_rhythm["violin"]`, `cello_bars = preset.duet_bars["cello"]`, `violin_bars = preset.duet_bars["violin"]` — velocities (82/70, 68/58, 74/62) and tempo (76/64/88) stay as literal arguments in each script's existing `add_measure(..., velocity=N)` and `tempo.MetronomeMark(number=N)` calls (these are not part of `MoodPreset`'s duet fields per the schema decided in Task 1; only rhythm/bars moved). After all 5 scripts are modified, run `.venv/bin/python3 -m pytest tests/test_golden_regression.py -v` (from Plan 01-03) to confirm the MIDI hashes and normalized-MusicXML hashes still match `tests/golden/baseline_hashes.json` captured before this data move — this is the direct proof of Roadmap Phase 1 success criterion 3.
  </action>
  <acceptance_criteria>
    - `grep -c "GENRE_PRESETS\s*=\|GENRE_PRESETS:\s*dict\|GENRE_IDEAS\s*=\|GENRE_IDEAS:\s*dict" scripts/generate_cello_dark_ostinato.py scripts/harmony_advisor.py` returns 0 (both dict literals fully removed, not just emptied)
    - `grep -c "class GenrePreset" scripts/generate_cello_dark_ostinato.py` returns 0 (superseded by `core.models.MoodPreset`)
    - `grep -c "from core.presets.registry import" scripts/generate_cello_dark_ostinato.py scripts/harmony_advisor.py scripts/generate_sexy_duet_loop.py scripts/generate_simple_sexy_duet_loop.py scripts/generate_dorian_sexy_duet_loop.py` returns 1 for each of the 5 files
    - `.venv/bin/python3 -m pytest tests/test_golden_regression.py -v` shows 2 passed (MIDI hashes AND normalized MusicXML hashes both match Plan 03's pre-move baseline — proves the data move is a byte-for-byte no-op)
    - `.venv/bin/python3 scripts/generate_cello_dark_ostinato.py --list-genres` runs with exit 0 and lists all 4 solo mood names with feel text (proves the CLI still works end-to-end)
    - `.venv/bin/python3 scripts/harmony_advisor.py --genre ritual_tribal` runs with exit 0 and prints progressions/modulations/mood sections (proves harmony_advisor.py's data-source swap works)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_golden_regression.py tests/test_presets_registry.py -v && .venv/bin/python3 scripts/generate_cello_dark_ostinato.py --list-genres && .venv/bin/python3 scripts/harmony_advisor.py --genre ritual_tribal</automated>
  </verify>
  <done>All 5 scripts import preset data from core/presets/ instead of defining it inline; golden regression test passes 2/2 against the pre-move baseline, proving the migration is a behavioral no-op; both CLI scripts run end-to-end with exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| core/presets/mood_presets.py (developer-authored data) ↔ scripts/*.py (CLI consumers) | Data-only boundary; no external/untrusted input in this phase — data is migrated from trusted, git-committed source files |
| core/presets/registry.py ↔ callers | `get_preset()` lookup by string key; miss produces a natural `KeyError`, not a silent fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-08 | Tampering | Data migration silently altering preset values (e.g. "fixing" the A1 out-of-range note) | mitigate | Task 1 explicitly requires verbatim migration with an inline flag comment (not a silent fix); Task 2's golden regression test is the enforcement mechanism — any accidental data alteration fails the MIDI hash comparison |
| T-01-09 | Repudiation | `get_preset()` KeyError on typo'd mood name giving an unclear failure | accept | Natural Python `KeyError` on dict miss is sufficient for this phase's scope (developer-facing CLI, not end-user input); CONTEXT.md/PATTERNS.md explicitly choose "keep it simple" over a custom exception type |
| T-01-10 | Tampering | Script behavior silently changing during the data-source swap (e.g. wrong duet preset name looked up) | mitigate | Task 2's acceptance criteria requires both the golden regression test (byte-level proof) AND manual CLI smoke runs (`--list-genres`, `--genre ritual_tribal`) to pass before the task is done |
</threat_model>

<verification>
Run `.venv/bin/python3 -m pytest tests/ -v` (full suite, all 4 plans combined) — expect all tests green: test_models.py (5), test_import_boundary.py (2), test_validators.py (13+), test_golden_regression.py (2), test_presets_registry.py (7). Run `.venv/bin/python3 scripts/generate_cello_dark_ostinato.py --list-genres`, `.venv/bin/python3 scripts/harmony_advisor.py --list-genres`, and each duet script with no args — all exit 0.
</verification>

<success_criteria>
- MOOD_PRESETS registry contains all 7 entries (4 solo + 3 duet), data migrated verbatim from all 5 source scripts
- All 5 CLI scripts produce identical output (MIDI hash + normalized MusicXML hash) after the data-source swap, verified against Plan 03's pre-move baseline (Roadmap Phase 1 criterion 3)
- The pre-existing out-of-range A1 note is preserved verbatim and flagged, not silently fixed
</success_criteria>

<output>
Create `.planning/phases/01-core-library-skeleton-validators/01-04-SUMMARY.md` when done
</output>
