---
phase: 01-core-library-skeleton-validators
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - requirements.txt
  - tests/golden/baseline_hashes.json
  - tests/test_golden_regression.py
autonomous: true
requirements: [PLAT-03]

must_haves:
  truths:
    - "requirements.txt pins music21==10.5.0 and installs cleanly on Python 3.12+ (Roadmap Phase 1 success criterion 6)"
    - "A golden baseline of all 5 scripts' output, captured on music21==10.5.0, exists BEFORE any preset data moves to core/presets/ (CONTEXT.md-mandated sequencing)"
    - "The golden-file guard hashes MIDI output directly (verified byte-identical across runs) and uses a normalized/content-based comparison for MusicXML (verified non-deterministic due to object-id-based XML ids and encoding-date)"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned music21==10.5.0 and pytest dependency"
      contains: "music21==10.5.0"
    - path: "tests/golden/baseline_hashes.json"
      provides: "MIDI SHA1 hashes + normalized MusicXML content hashes for all 5 scripts' output, captured on the 10.5.0 baseline before data migration"
    - path: "tests/test_golden_regression.py"
      provides: "Automated comparison of current script output against the captured baseline; normalizes MusicXML before hashing"
      exports: ["test_midi_outputs_match_baseline", "test_musicxml_outputs_match_baseline_normalized"]
  key_links:
    - from: "tests/test_golden_regression.py"
      to: "tests/golden/baseline_hashes.json"
      via: "json.load of the baseline file"
      pattern: "baseline_hashes\\.json"
    - from: "tests/test_golden_regression.py"
      to: "scores/midi/*.mid, scores/musicxml/*.musicxml"
      via: "re-running the 5 scripts as subprocesses, then hashing their fresh output"
      pattern: "subprocess\\.run"
---

<objective>
Bump `requirements.txt` to the approved `music21==10.5.0` pin, and capture a golden regression baseline (MIDI SHA1 hashes + normalized MusicXML content) of all 5 existing CLI scripts' output BEFORE any preset data is moved into `core/presets/` in Plan 04 — establishing the "no-op relative to the 10.5.0 baseline" comparison point that CONTEXT.md requires.

Purpose: RESEARCH.md verified live that `requirements.txt` currently pins `music21>=9.1,<10` even though the actual `.venv` already runs 10.5.0 — the pin itself has never been committed. It also verified that raw-byte-hashing MusicXML produces false "regressions" on every run (object-id and encoding-date volatility) while MIDI output is fully deterministic. This plan produces the trustworthy baseline and comparison mechanism that Plan 04's data-move must pass against, without which Roadmap Phase 1 success criterion 3 ("all existing CLI scripts still produce the same output files after refactoring") cannot be verified correctly.
Output: `requirements.txt` (updated pin), `tests/golden/baseline_hashes.json` (captured baseline), `tests/test_golden_regression.py` (comparison test, run again after Plan 04's data move).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-library-skeleton-validators/01-CONTEXT.md
@.planning/phases/01-core-library-skeleton-validators/01-RESEARCH.md
@.planning/phases/01-core-library-skeleton-validators/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump requirements.txt to music21==10.5.0 and verify clean install</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current content: single line `music21>=9.1,<10`)
    - .planning/phases/01-core-library-skeleton-validators/01-RESEARCH.md (Pitfall 2: requirements.txt currently pins the wrong major version; the .venv already has 10.5.0 installed from the research session, but the file itself has not been updated)
  </read_first>
  <action>
    Replace the single line in `requirements.txt` with two pinned lines: `music21==10.5.0` and `pytest` (unpinned per RESEARCH.md Assumptions Log A1 — CONTEXT.md does not lock a specific pytest version, and RESEARCH.md verified `pytest 9.1.1` installs cleanly with no conflicts alongside `music21==10.5.0`). Verify the install is clean by running `python3 -m pip install -r requirements.txt` against the repo's `.venv` (use `.venv/bin/python3 -m pip install -r requirements.txt`, never a bare `pip` command per RESEARCH.md Environment Availability finding that bare `pip` is not on PATH in this shell) and confirm zero errors and the installed version matches `music21==10.5.0` via `.venv/bin/python3 -c "import music21; assert music21.__version__ == '10.5.0'"`.
  </action>
  <acceptance_criteria>
    - `cat requirements.txt` shows exactly `music21==10.5.0` and `pytest` as the two pinned entries (no `>=9.1,<10` remnant)
    - `.venv/bin/python3 -m pip install -r requirements.txt` exits 0 with no error output
    - `.venv/bin/python3 -c "import music21; print(music21.__version__)"` prints `10.5.0`
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python3 -m pip install -r requirements.txt && .venv/bin/python3 -c "import music21; assert music21.__version__ == '10.5.0', music21.__version__; print('OK 10.5.0')"</automated>
  </verify>
  <done>requirements.txt pins music21==10.5.0 and pytest; clean install verified against the actual .venv.</done>
</task>

<task type="auto">
  <name>Task 2: Capture golden baseline (MIDI hash + normalized MusicXML) from all 5 scripts on the 10.5.0 pin</name>
  <files>tests/golden/baseline_hashes.json, tests/test_golden_regression.py</files>
  <read_first>
    - .planning/phases/01-core-library-skeleton-validators/01-RESEARCH.md (Pitfall 1: full root-cause explanation of MusicXML id volatility — `Music21Object.id` defaults to `builtins.id(self)`; MIDI verified byte-identical across runs, MusicXML verified NON-identical even with zero data change; recommended fix is regex-stripping `<encoding-date>...</encoding-date>` and all `id="P..."`/`id="I..."` attribute values before hashing MusicXML)
    - scripts/generate_cello_dark_ostinato.py (its `main()` writes `scores/musicxml/cello_{genre}_ostinato.musicxml` and `scores/midi/cello_{genre}_ostinato.mid` per invocation; must be run once per genre: dark_trip_hop, ritual_tribal, noir_slow_burn, driving_cinematic — 4 invocations, 8 output files)
    - scripts/generate_sexy_duet_loop.py, scripts/generate_simple_sexy_duet_loop.py, scripts/generate_dorian_sexy_duet_loop.py (each writes exactly one fixed-name MusicXML + MIDI pair with no CLI args needed — 3 invocations, 6 output files)
  </read_first>
  <action>
    Create `tests/golden/` directory. Write a baseline-capture routine (either inline in `tests/test_golden_regression.py` as a `capture_baseline()` helper invoked by a one-time script run, or as a small standalone capture step documented in the test file's module docstring) that: (1) runs `.venv/bin/python3 scripts/generate_cello_dark_ostinato.py --genre {g}` for each of the 4 genres (`dark_trip_hop`, `ritual_tribal`, `noir_slow_burn`, `driving_cinematic`) plus the 3 duet scripts with no args, for a total of 8 MIDI files and 8 MusicXML files under `scores/midi/` and `scores/musicxml/`; (2) computes SHA1 of each `.mid` file's raw bytes; (3) normalizes each `.musicxml` file by stripping the `<encoding-date>...</encoding-date>` element and all `id="P..."`/`id="I..."` attribute values via regex substitution, then computes SHA1 of the normalized text; (4) writes both hash sets to `tests/golden/baseline_hashes.json` keyed by output filename, e.g. `{"midi": {"cello_dark_trip_hop_ostinato.mid": "<sha1>", ...}, "musicxml_normalized": {"cello_dark_trip_hop_ostinato.musicxml": "<sha1>", ...}}`. Execute this capture NOW (before Plan 04 moves any preset data) so `tests/golden/baseline_hashes.json` reflects the pre-migration, 10.5.0-pinned state — this is the literal "capture goldens BEFORE moving data" step CONTEXT.md requires. Then write `tests/test_golden_regression.py` with two test functions: `test_midi_outputs_match_baseline` (re-runs all 5 scripts, re-hashes MIDI output, compares against `baseline_hashes.json["midi"]`, asserts exact match) and `test_musicxml_outputs_match_baseline_normalized` (re-runs, re-normalizes, re-hashes MusicXML, compares against `baseline_hashes.json["musicxml_normalized"]`, asserts exact match) — both marked with a comment explaining that these tests are EXPECTED to still pass at the end of THIS plan (data hasn't moved yet, so scripts are unchanged) and are the guard Plan 04 must keep green after the data-move.
  </action>
  <acceptance_criteria>
    - `tests/golden/baseline_hashes.json` exists and is valid JSON containing exactly 8 entries under `"midi"` and 8 entries under `"musicxml_normalized"`
    - `.venv/bin/python3 -m pytest tests/test_golden_regression.py -v` shows 2 passed, 0 failed (run immediately after capture, before any data has moved — this proves the comparison mechanism itself is self-consistent)
    - Re-running `.venv/bin/python3 scripts/generate_cello_dark_ostinato.py --genre dark_trip_hop` twice in a row and diffing the two `.mid` file SHA1 hashes shows them IDENTICAL (confirms MIDI determinism claim from RESEARCH.md)
    - The normalization regex in `tests/test_golden_regression.py` strips both `<encoding-date>` content and `id="P...\"`/`id="I..."` attributes (grep confirms both patterns are handled, not just one)
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_golden_regression.py -v</automated>
  </verify>
  <done>tests/golden/baseline_hashes.json captures MIDI SHA1 + normalized-MusicXML SHA1 for all 8 output files on the music21==10.5.0 pin, before any preset data has moved; tests/test_golden_regression.py passes against this baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Golden baseline file (tests/golden/baseline_hashes.json) ↔ regression test | The baseline is developer-captured, committed to git, and read back by the test — no external/untrusted input; risk is accidental staleness, not tampering |
| requirements.txt package pin | Supply-chain trust boundary for music21/pytest — both already vetted in RESEARCH.md's Package Legitimacy Audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-06 | Tampering | tests/golden/baseline_hashes.json going stale/silently wrong | mitigate | Baseline is captured via an explicit, documented, re-runnable routine (not a one-off manual step) so it can be regenerated deliberately if a future intentional data change requires it; the test file's module docstring documents the recapture procedure |
| T-01-07 | Repudiation | Golden regression false-positive on MusicXML raw bytes | mitigate | RESEARCH.md's Pitfall 1 root-cause is directly addressed: normalization strips `<encoding-date>` and `id="P.../I..."` attributes before hashing, per the verified-live finding that raw byte hashing fails even with zero data change |
| T-01-SC | Tampering | pip install of music21/pytest (npm/pip supply-chain) | mitigate | Both packages already cleared RESEARCH.md's Package Legitimacy Audit (slopcheck [OK], long-established foundational libraries) — no new checkpoint needed since no new package is introduced beyond what RESEARCH.md already vetted |
</threat_model>

<verification>
Run `.venv/bin/python3 -m pytest tests/test_golden_regression.py -v` — expect 2 passed. Run `.venv/bin/python3 -m pip install -r requirements.txt` — expect clean exit 0. Confirm `tests/golden/baseline_hashes.json` has 8+8 entries via `python3 -c "import json; d=json.load(open('tests/golden/baseline_hashes.json')); assert len(d['midi'])==8 and len(d['musicxml_normalized'])==8"`.
</verification>

<success_criteria>
- requirements.txt pins music21==10.5.0, installs cleanly (Roadmap Phase 1 criterion 6)
- Golden baseline captured BEFORE any preset data move, on the 10.5.0 pin, using MIDI-hash-primary + normalized-MusicXML-secondary comparison (per RESEARCH.md's verified findings)
- Golden regression test is self-consistent and ready to be the pass/fail gate for Plan 04's data migration
</success_criteria>

<output>
Create `.planning/phases/01-core-library-skeleton-validators/01-03-SUMMARY.md` when done
</output>
