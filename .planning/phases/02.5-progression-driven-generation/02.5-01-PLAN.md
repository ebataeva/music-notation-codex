---
phase: 02.5-progression-driven-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - core/engine/progression.py
  - core/engine/loop_engine.py
  - tests/test_progression.py
  - tests/test_loop_engine.py
autonomous: true
requirements: [INPUT-01, TRACE-01]

must_haves:
  truths:
    - "A progression string like 'Am F C G' parses into chord objects; an unrecognized token produces a clear, human-readable error naming the bad token (ROADMAP 2.5 SC1)"
    - "LoopEngine.build_score() with an arbitrary parsed progression + mood returns a Score whose bars pass validate_pitch and validate_bar_duration (ROADMAP 2.5 SC2)"
    - "Generated bars use chord tones of the user's progression, voiced monophonically in idiomatic cello register C2-D5 (ROADMAP 2.5 SC3)"
    - "The GenerationTrace records which chord tones, register choices, and pattern strategy were used per bar (ROADMAP 2.5 SC4)"
    - "The full pytest suite including the Phase 1/2 golden regression baseline stays green"
  artifacts:
    - path: "core/engine/progression.py"
      provides: "Progression parser: chord text -> chord objects with per-token error reporting"
      contains: "raise ValueError"
    - path: "core/engine/loop_engine.py"
      provides: "Progression-driven generation path wired into build_score/generate_variant"
    - path: "tests/test_progression.py"
      provides: "Parser tests: valid progressions, named bad-token errors"
  key_links:
    - from: "core/engine/loop_engine.py"
      to: "core/engine/progression.py"
      via: "import of the parser"
      pattern: "from core\\.engine\\.progression import"
    - from: "tests/test_progression.py"
      to: "core/engine/progression.py"
      via: "import of the parser"
      pattern: "from core\\.engine\\.progression import"
---

<objective>
Implement progression-driven generation per ROADMAP.md Phase 2.5: chord-progression parsing ("Am F C G" -> chord objects via pychord) -> chord-tone extraction -> cello-register mapping (C2-D5, monophonic, idiomatic) -> rhythm-strategy application (existing MoodPreset strategies decide *when* notes land; this phase decides *which* pitch) -> validated bar generation with full per-bar GenerationTrace.

Purpose: the engine stops being limited to hardcoded preset bars — an arbitrary user progression + mood produces a playable, validated cello loop. This is the hardest musical-algorithm work in the project and the foundation for Phase 3 (TheoryExplainer grounds its "why it works" text in the trace this phase populates).
Output: `core/engine/progression.py` (new parser), progression-driven path in `core/engine/loop_engine.py`, `pychord` pinned in requirements.txt, tests proving all four ROADMAP success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-loopengine-exportengine/02-PATTERNS.md
@.planning/phases/02-loopengine-exportengine/02-02-SUMMARY.md
@.planning/phases/02-loopengine-exportengine/02-03-SUMMARY.md
</context>

<autonomy_contract>
Run this plan end-to-end without stopping for confirmation, EXCEPT at the escalation triggers below. Write code -> run tests -> read failures -> fix -> re-run, without asking permission per step.

**Iteration limit:** max 3 fix attempts per failing test. On the 4th failure of the same test, stop and return a checkpoint — do not keep guessing.

**Blast radius:** touch only `core/engine/`, `core/theory/` (if chord-tone logic belongs there), `tests/`, and the `pychord` line in `requirements.txt`. Do NOT modify `core/models.py`, `core/presets/`, or anything in `scripts/`. `GenerationTrace` in `core/models.py` already has the needed fields (`pattern_strategy`, `register_choices`, `chord_tones_used`, `voice_leading_steps`, `seed`) — construct instances, do not edit `models.py`.

**Escalation triggers (return a structured checkpoint instead of proceeding):**
- A design decision isn't determined by the roadmap or existing code (e.g. how to voice a chord with more than 4 tones in a monophonic line — no single obviously-correct answer).
- Same test fails 4 times in a row.
- Fixing this phase would require changing a Phase 1/2 file outside the allowed blast radius.
- pychord can't represent a chord type the user is likely to type (e.g. slash chords, sus chords) — scope narrowing vs. custom parsing is a scope decision, not a bug fix.
</autonomy_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pychord dependency + progression parser with named bad-token errors</name>
  <files>requirements.txt, core/engine/progression.py, tests/test_progression.py</files>
  <read_first>
    - requirements.txt (current pins — add pychord alongside, matching pin style)
    - core/engine/validators.py (ValueError-only error convention; no custom exception classes anywhere in the codebase)
    - core/models.py (GenerationRequest / dataclass shapes — read-only, do not modify)
  </read_first>
  <behavior>
    - Test: `parse_progression("Am F C G")` returns 4 chord objects in input order, each exposing its component tones (e.g. Am -> A, C, E)
    - Test: chords with qualities parse: "F#m7", "Gsus4", "Cmaj7", "Bdim"
    - Test: an unrecognized token raises ValueError whose message names the exact bad token (e.g. "Xz9" appears verbatim in the message), not a generic parse failure
    - Test: empty/whitespace-only input raises ValueError with a clear message
    - Test: surrounding/duplicate whitespace is tolerated ("Am  F   C G" parses the same as "Am F C G")
  </behavior>
  <action>
    Add `pychord` to requirements.txt, pinned to the current stable version (0.2.8 per the project tech-stack doc), and confirm it installs cleanly into .venv. Create `core/engine/progression.py` with a `parse_progression(text: str)` function: split the input on whitespace, parse each token via `pychord.Chord`, and collect results preserving order. Wrap pychord's own parse failure per token and re-raise as `ValueError` naming the offending token verbatim (ValueError only — codebase-wide convention, no custom exception classes). Return chord objects (or a thin dataclass wrapping name + component tones) that downstream register-mapping can consume without importing pychord directly.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_progression.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_progression.py -v` passes with zero failures
    - `pip show pychord` (in .venv) confirms the pinned version installed
    - A bad token's exact text appears in the raised ValueError message (asserted in a test)
  </acceptance_criteria>
  <done>parse_progression turns "Am F C G" into ordered chord objects with component tones; unrecognized tokens raise ValueError naming the token; pychord is pinned and installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chord-tone -> cello-register mapping wired into LoopEngine with per-bar GenerationTrace</name>
  <files>core/engine/loop_engine.py, tests/test_loop_engine.py</files>
  <read_first>
    - core/engine/loop_engine.py (existing build_score/generate_variant/_resolve_seed structure, SAFE-02 bar-count guard, validator wiring — extend, do not restructure)
    - core/models.py (GenerationTrace fields, GenerationRequest, LoopVariant — read-only)
    - core/presets/mood_presets.py (rhythm/pattern strategies — the source of *when* notes land)
    - core/engine/validators.py (validate_pitch default range C2-D5; extended=True widens it — cello parts use the default range)
  </read_first>
  <behavior>
    - Test: build_score with a parsed progression ("Am F C G") + a solo mood preset returns a music21 Score with one bar per chord (or per the preset's bar strategy), every pitch passing validate_pitch (default cello range) and every bar passing validate_bar_duration
    - Test: every generated bar uses only chord tones of that bar's chord from the input progression (compare pitch classes against the parsed chord's components)
    - Test: output is monophonic — no chords/intervals, one note at a time
    - Test: the returned variant's trace has non-empty pattern_strategy, and register_choices / chord_tones_used with exactly one entry per bar
    - Test: same seed + same progression + same mood reproduces an identical result; different seeds may differ
    - Test: register mapping favors root/fifth in the low register and avoids leaps larger than an octave between consecutive notes (assert max interval)
  </behavior>
  <action>
    Add a chord-tone -> cello-register mapping step to `core/engine/loop_engine.py` (or a helper module within core/engine/): given a chord's tones, choose concrete pitches within C2-D5, monophonic, idiomatic for cello — favor root/fifth in the low register, avoid awkward leaps. Use the existing MoodPreset rhythm/pattern strategies as the source of *when* notes land; this step only decides *which* pitch. Wire the mapped pitches into the existing build_score/generate_variant path so an arbitrary parsed progression + mood produces a Score, reusing the existing seed plumbing (_resolve_seed) and SAFE-02 bar-count guard. Populate GenerationTrace per bar: chord tones chosen, register decision, pattern strategy used — construct GenerationTrace instances, do not edit core/models.py. Preserve the existing preset-only code path unchanged (golden regression must stay byte-identical).
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_loop_engine.py tests/test_progression.py -v</automated>
  </verify>
  <acceptance_criteria>
    - All new progression-driven tests pass
    - All pre-existing tests in tests/test_loop_engine.py still pass unchanged (preset path untouched)
    - Generated bars pass validate_pitch and validate_bar_duration (asserted in tests, not just not-raising)
  </acceptance_criteria>
  <done>An arbitrary parsed progression + mood produces a validated, monophonic, cello-idiomatic Score with a fully populated per-bar GenerationTrace; the preset-only path is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: full-suite regression against the Phase 1/2 golden baseline</name>
  <files>tests/ (read-only verification, no modification expected)</files>
  <read_first>
    - tests/test_golden_regression.py (byte-identity guard for all 7 script invocations)
  </read_first>
  <action>
    Run `pytest tests/` in full — not just the new tests — to confirm no regression against the Phase 1/2 golden baseline (all 7 script invocations byte-identical). If the golden suite touches tracked scores/musicxml files (known IN-04 side effect), discard with targeted `git checkout -- scores/`. If any regression is found, fix it within this plan's blast radius; if the fix would require touching a Phase 1/2 file outside the blast radius, stop and return a checkpoint per the autonomy contract.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/` reports zero failures across the entire suite
    - Both golden regression tests pass (MIDI byte-identity, MusicXML normalized identity)
  </acceptance_criteria>
  <done>Entire suite green: new progression-driven tests plus all Phase 1/2 tests including golden regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User chord text -> parser | Free-form user input crosses into pychord parsing; malformed tokens must fail closed with ValueError, never crash with a raw library exception |
| Parsed chords -> engine | Chord tones flow into register mapping and score assembly; validators (D-06) gate every generated pitch/bar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.5-01 | Tampering | parse_progression input | mitigate | Per-token parse with ValueError naming the bad token; no raw pychord exception escapes |
| T-02.5-02 | Denial of Service | Oversized progression input | mitigate | Existing SAFE-02 bar-count guard (>64 bars raises before Score construction) applies to the progression path |
| T-02.5-SC | Tampering | pychord install | accept | Pinned version from PyPI; single new pure-Python dependency, no transitive native code |
</threat_model>

<verification>
Run `.venv/bin/python3 -m pytest tests/ -v` — expect zero failures across the full suite, including both golden regression tests. All four ROADMAP Phase 2.5 Success Criteria must be TRUE against the actual codebase.
</verification>

<success_criteria>
- "Am F C G" parses into chord objects; an unrecognized token produces a clear error naming the bad token.
- build_score with an arbitrary parsed progression + mood returns a Score whose bars pass validate_pitch and validate_bar_duration.
- Generated bars use chord tones of the input progression, voiced monophonically in C2-D5.
- GenerationTrace records chord tones, register choices, and pattern strategy per bar.
- Full test suite including golden regression passes.
</success_criteria>

<output>
Create `.planning/phases/02.5-progression-driven-generation/02.5-01-SUMMARY.md` when done.
Include a one-paragraph summary of any judgment calls made during pitch-mapping (this is the phase most likely to need a human ear-check afterward).
</output>
