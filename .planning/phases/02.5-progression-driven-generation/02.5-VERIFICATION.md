---
phase: 02.5-progression-driven-generation
verified: 2026-07-06T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
mvp_mode_discrepancy: "ROADMAP.md declares Mode: mvp for this phase, but the phase goal is not in User Story format. Same situation as Phase 1: pure engine-level work with no user-facing surface until Phase 4. The UAT (02.5-UAT.md) records an explicit user decision (2026-07-05) to proceed with standard deliverable-based verification instead of user-flow framing. Standard goal-backward verification against the roadmap's 4 Success Criteria was used, consistent with the 01-VERIFICATION.md precedent."
context_note: "Retroactive verification (2026-07-06). Phase 2.5 was closed on 2026-07-04 via UAT (8/8 passed), Nyquist VALIDATION (nyquist_compliant: true), and the interim v1 milestone audit's integration check, without the standard gsd-verifier artifact — logged as tech debt in v1-MILESTONE-AUDIT.md. This report closes that debt by verifying against the current main-tree state (HEAD 2030293), which includes the post-phase review-fix commits (567e192, 7b212be, afaeb4f)."
---

# Phase 2.5: Progression-Driven Generation Verification Report

**Phase Goal:** The engine generates a playable, validated cello loop from an arbitrary user chord progression ("Am F C G") plus key/mood — not just from hardcoded preset bars: pychord parsing → chord tones → mapping into the cello register → applying the preset's rhythm/pattern strategies → range- and duration-validated bars.
**Verified:** 2026-07-06 (retroactive — see context_note)
**Status:** passed
**Re-verification:** No — initial (retroactive) verification

## MVP Mode Note

ROADMAP.md marks this phase `Mode: mvp`, but the goal text is not in User Story format. 02.5-UAT.md already records this exact deviation with an explicit user decision (2026-07-05) to proceed with standard verification, because the phase is purely engine-level (parsing + generation) with no UI surface until Phase 4. Standard goal-backward verification against the roadmap's explicit Success Criteria was used, matching the Phase 1 precedent. Surfaced as a process note, not a gap.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria + plan frontmatter)

All evidence below was obtained by executing the code directly against the current main tree (HEAD 2030293) — not taken from SUMMARY.md claims.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | "Am F C G" parses into chord objects; an unrecognized token produces a clear, human-readable error naming the bad token (SC1, INPUT-01) | VERIFIED | Live run: `parse_progression("Am F C G")` → `[('Am', [A,C,E]), ('F', [F,A,C]), ('C', [C,E,G]), ('G', [G,B,D])]` in input order. `parse_progression("Am Xz9 C")` → `ValueError: Could not parse chord token 'Xz9' in progression 'Am Xz9 C': ...` — bad token named verbatim. Empty/whitespace input raises a clear ValueError. 9 tests in `tests/test_progression.py`, all passing. |
| 2 | `build_progression_score()` with an arbitrary parsed progression + mood returns a Score whose bars pass validate_pitch and validate_bar_duration (SC2) | VERIFIED | Live run: `build_progression_score(chords, get_preset("dark_trip_hop"), seed=42)` → 4 measures (one per chord); every note re-validated with `validate_pitch` (all MIDI 36–74 = C2–D5) and every bar's durations re-validated with `validate_bar_duration` against the preset meter — zero exceptions. `core/engine/loop_engine.py:461` calls `validate_pitch` per note; `:438` calls `validate_bar_duration`. |
| 3 | Generated bars use chord tones of the user's progression, voiced monophonically in idiomatic cello register C2–D5 (SC3) | VERIFIED | Live run: every note's pitch class is in that bar's `ParsedChord.components` (checked per measure); no two notes share an offset within a measure (monophonic); all pitches within MIDI 36–74. Register mapping (`_choose_register_for_chord_tone`, loop_engine.py:307-352) biases root/fifth to octaves ≤3, constrains leaps to ≤12 semitones (WR-01 fix: leap rule narrows the candidate set, bias applied inside it — no ceiling-parking). Tests: chord-tone restriction, monophony, ≤octave leaps over 16 bars, non-monotonic movement, flat-key respelling (IN-02), power chords (IN-03). |
| 4 | GenerationTrace records which chord tones, register choices, and pattern strategy were used per bar (SC4, TRACE-01) | VERIFIED | Live run: `generate_variant_from_progression(chords, preset, seed=42).trace` → `pattern_strategy="progression_driven_register_mapped"`, `register_choices` = 4 entries (one per bar, e.g. `['low register','low register','low register','mid register']`), `chord_tones_used` = 4 per-bar tone lists matching the parsed progression, `seed=42` recorded; no-seed call still records a concrete seed. Trace construction at loop_engine.py:501-507. |
| 5 | The full pytest suite including the Phase 1/2 golden regression baseline stays green (plan frontmatter truth 5) | VERIFIED | `.venv/bin/python3 -m pytest tests/ -q` → **94 passed, 1 warning** (expected D-07 legacy-note warning). Golden regression isolated: `pytest tests/test_golden_regression.py -q` → 2/2 (byte-identical MIDI + normalized MusicXML). Phase-scoped: `pytest tests/test_progression.py tests/test_loop_engine.py -q` → 40 passed. |

**Score:** 5/5 truths verified

### Required Artifacts (plan frontmatter must_haves)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/engine/progression.py` | Progression parser: chord text → chord objects with per-token error reporting; contains `raise ValueError` | VERIFIED | Read in full (91 lines, substantive): `ParsedChord` frozen dataclass, `parse_progression()`, `_normalize_token()` quality-alias normalization (maj7→M7 etc.), 3 `raise ValueError` sites, CR-01 slash-token rejection guard before pychord is ever touched. No pychord types leak downstream. |
| `core/engine/loop_engine.py` | Progression-driven generation path wired in | VERIFIED | `_all_candidate_pitches`, `_choose_register_for_chord_tone`, `_register_map_chord`, `_respell_pitch_to_key`, `build_progression_score`, `generate_variant_from_progression` all present and substantive (loop_engine.py:276-520); parallel to — not modifying — the preset-verbatim `build_score`/`generate_variant` path. |
| `tests/test_progression.py` | Parser tests: valid progressions, named bad-token errors | VERIFIED | 9 tests incl. ordered parsing, component tones, extended qualities, bad-token naming, empty/whitespace, slash rejection + cache-safety regression. All pass. |
| `requirements.txt` | `pychord` pinned | VERIFIED | Line 2: `pychord==0.2.8`; `pip show pychord` in .venv confirms 0.2.8 installed. |
| `tests/test_loop_engine.py` | Progression-driven engine tests | VERIFIED | 13 progression-path tests (one-bar-per-chord, validators, chord-tone restriction, monophony, trace, seed reproducibility, leap guard, non-monotonic, flat-key, power chords, duet-only guard, preset-path regression guard). All pass. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `core/engine/loop_engine.py` | `core/engine/progression.py` | `from core.engine.progression import ParsedChord` | WIRED | loop_engine.py:15; `ParsedChord` consumed by `_register_map_chord`/`build_progression_score` type signatures and logic. |
| `tests/test_progression.py` | `core/engine/progression.py` | `from core.engine.progression import parse_progression` | WIRED | test_progression.py:5; 9 tests exercise the real parser. |
| `tests/test_loop_engine.py` | progression path | `parse_progression` + `build_progression_score`/`generate_variant_from_progression` imports | WIRED | 14 in-test imports; tests build real Scores/variants, not mocks. |
| `build_progression_score` | `core/engine/validators.py` | `validate_pitch` per note, `validate_bar_duration` per rhythm | WIRED | loop_engine.py:438, 461 — validators gate every generated pitch/bar (D-06). |
| `generate_variant_from_progression` | `core/models.py` GenerationTrace | Trace constructed, models.py untouched | WIRED | loop_engine.py:501-507; blast radius respected (GenerationTrace fields pre-existed from Phase 1). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `generate_variant_from_progression()` | `trace.chord_tones_used` | `ParsedChord.components` from real pychord parse of user text | Yes — live run shows per-bar tone lists matching the input progression, not hardcoded | FLOWING |
| `generate_variant_from_progression()` | `trace.register_choices` | `_classify_register` over actually-generated measure pitches | Yes — real per-bar labels derived from the built Score | FLOWING |
| `build_progression_score()` | measure notes | `_register_map_chord` over parsed chord tones + preset rhythm | Yes — seed 7 twice → byte-identical MIDI; different content per progression | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full suite green | `.venv/bin/python3 -m pytest tests/ -q` | `94 passed, 1 warning in 4.35s` | PASS |
| Phase-scoped tests | `pytest tests/test_progression.py tests/test_loop_engine.py -q` | `40 passed` | PASS |
| Golden regression (preset path untouched) | `pytest tests/test_golden_regression.py -q` | `2 passed` | PASS |
| Parse + bad-token error | `parse_progression("Am Xz9 C")` | ValueError naming `'Xz9'` verbatim | PASS |
| CR-01 slash rejection + cache safety | `parse_progression("C/G F")` then `parse_progression("C F G Am")` | Slash rejected with clear error; subsequent C still `['C','E','G']` (root-first, cache uncorrupted) | PASS |
| SC2/SC3 live | `build_progression_score(Am F C G, dark_trip_hop, seed=42)` | 4 bars, all pitches C2–D5, monophonic, chord tones only, validators pass | PASS |
| SC4 live | `generate_variant_from_progression(...).trace` | strategy `progression_driven_register_mapped`, 4 register_choices, 4 chord_tones_used, seed recorded (also with no seed given) | PASS |
| Seed reproducibility | Two `build_progression_score(..., seed=7)` calls → MIDI bytes | Byte-identical | PASS |
| SAFE-02 on progression path | 65-chord progression | `ValueError: Requested 65 bars exceeds the maximum of 64.` before Score construction | PASS |
| pychord pin | `pip show pychord` | `Version: 0.2.8` matching `requirements.txt` | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| INPUT-01 | 02.5-01 | User can enter a chord progression as text (e.g. `Am F C G`) | SATISFIED | `parse_progression()` turns free text into ordered chord objects with named bad-token errors; extended qualities (F#m7, Gsus4, Cmaj7, Bdim) parse; slash chords fail closed (CR-01). Engine-level only — UI text input is Phase 4 scope per ROADMAP. |
| TRACE-01 | 02.5-01 | Every generated variant carries a GenerationTrace (seed, pattern strategy, register choices, voice-leading steps, chord tones used) | SATISFIED | Both generation paths populate a trace; progression path verified live with seed + strategy + per-bar register_choices/chord_tones_used. `voice_leading_steps` is explicitly `None` on the progression path (documented "future refinement", loop_engine.py:505) — the requirement lists the field, which exists on the dataclass and is populated as null-by-design; ROADMAP SC4 (the phase contract) requires chord tones, register choices, and pattern strategy only, all populated. See INFO note below. |

No orphaned requirements: REQUIREMENTS.md maps exactly INPUT-01 and TRACE-01 to Phase 2.5 (both marked Complete in the 2026-07-06 audit), and both are declared in the plan's `requirements:` frontmatter.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers in any phase-modified file (`core/engine/progression.py`, `core/engine/loop_engine.py`, `tests/test_progression.py`, `tests/test_loop_engine.py`, `requirements.txt`) | — | grep confirmed zero matches |
| `core/engine/loop_engine.py` | 505 | `voice_leading_steps=None  # Explicit step-interval trace is a future refinement.` | INFO | Not a stub — deliberate, documented null on the progression path; the actual voice-leading behavior (≤octave leaps, continuity) is implemented and tested. Phase 3's TheoryExplainer consumes `register_choices`/`chord_tones_used`/`pattern_strategy`, all populated. Worth populating when Phase 3 needs step intervals. |
| `tests/test_validation_phase2.py` | — | Untracked file in main tree (6 tests), not part of this phase; explains 94 vs the documented 88 suite count | INFO | Environment noise from ongoing tech-debt work (HEAD 2030293 is a pre-dispatch planning commit; a concurrent agent is working in a worktree). Its 6 tests pass and do not affect phase 2.5 verification. |
| `scores/musicxml/*.musicxml` | — | 7 files locally modified (volatile encoding-date/object-id churn) | INFO | Known IN-04 side effect of running the generator scripts/golden suite; present before this verification ran and re-triggered by it. Content-identical per the normalized-hash golden test passing. Left as found — this verification changes no repo files besides this report. |

### Post-Phase Fix Verification (02.5-REVIEW / 02.5-REVIEW-FIX)

The phase's code review found 1 critical + 3 warnings + 4 info (`02.5-REVIEW.md`, status issues_found); `02.5-REVIEW-FIX.md` claims all 8 fixed (commits 567e192, 7b212be, afaeb4f). Independently confirmed in the current code, not from the fix report:

- **CR-01 (slash-chord cache corruption):** slash tokens rejected at progression.py:77-80 *before* reaching pychord; live cache-safety check passed (see spot-checks).
- **WR-01 (monotonic climb to D5):** leap rule now narrows candidates, register bias applied inside the set (loop_engine.py:335-350); `test_build_progression_score_does_not_monotonically_climb_to_ceiling` passes.
- **WR-02 (duet-only preset leak):** actionable guard at loop_engine.py:430-434 naming solo presets; test passes.
- **IN-02/IN-03:** `_respell_pitch_to_key` (flat-key respelling) and interval-based fifth detection both present and tested.

### Human Verification Required

None outstanding. The one inherently-human item for this phase — the subjective ear-check of register-mapping quality — was already performed and passed during UAT (02.5-UAT.md test 8: 4 MIDI files, 2 progressions x 2 presets, user listened and approved; commit b9557fe). No new human items arise from this verification.

### Gaps Summary

No gaps. All 4 ROADMAP Success Criteria plus the plan's fifth truth (full-suite green incl. golden regression) are independently verified TRUE against the current main-tree codebase by executing the code directly: free-text progressions parse with named bad-token errors, arbitrary progression + mood produces a validated (C2–D5, duration-checked), monophonic, chord-tone-only cello Score with one bar per chord, the GenerationTrace is fully populated per bar with real (flowing, non-hardcoded) data, seeds reproduce byte-identical MIDI, SAFE-02 applies to the progression path, and the preset-verbatim path remains byte-identical per the golden regression suite. Both phase requirements (INPUT-01, TRACE-01) are satisfied at engine level with no orphans. The post-phase review's critical finding (CR-01 pychord cache corruption) is confirmed fixed in the code, not just claimed fixed.

This report retroactively closes the v1-MILESTONE-AUDIT.md tech-debt item "phase 2.5 closed without the standard gsd-verifier artifact".

---

_Verified: 2026-07-06_
_Verifier: Claude (gsd-verifier)_
